// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 88299dc440e7


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: teams", () => {
  describe("teams", () => {
    it("POST /api/v1/teams/join_by_code returns successfully", async () => {
      const result = await client.v1.teams.joinByCode({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/join_by_code returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.teams.joinByCode({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/teams/join_by_code returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.joinByCode({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/join_by_code returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.joinByCode({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/join_by_code returns 429", async () => {
      const ec = errorClient(429);
      try {
        await ec.v1.teams.joinByCode({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(429);
      }
    });

    it("DELETE /api/v1/teams/{team} returns successfully", async () => {
      await client.v1.teams.delete("test-value");
    });

    it("DELETE /api/v1/teams/{team} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/teams/{team} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/teams/{team} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/teams/{team} returns successfully", async () => {
      const result = await client.v1.teams.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/teams/{team} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/teams/{team} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/teams/{team} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/teams/{team} returns successfully", async () => {
      const result = await client.v1.teams.update("test-value", { team: "test-value" });
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/teams/{team} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.update("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/teams/{team} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.update("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PATCH /api/v1/teams/{team} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.update("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/teams/{team} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.teams.update("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/teams/{team}/join returns successfully", async () => {
      const result = await client.v1.teams.join("test-value", { team: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/{team}/join returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.teams.join("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/teams/{team}/join returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.join("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/{team}/join returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.join("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/teams/{team}/join returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.join("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/teams returns successfully", async () => {
      const result = await client.v1.teams.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/teams returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/teams returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/teams returns successfully", async () => {
      const result = await client.v1.teams.create({ name: "test-name" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.create({ name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.create({ name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/teams returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.teams.create({ name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/teams/{team}/leave returns successfully", async () => {
      await client.v1.teams.leave("test-value");
    });

    it("DELETE /api/v1/teams/{team}/leave returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.leave("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/teams/{team}/leave returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.leave("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/teams/{team}/leave returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.leave("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/teams/{team}/leave returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.teams.leave("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/teams/{team}/invites returns successfully", async () => {
      const result = await client.v1.teams.invites("test-value", { team: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/{team}/invites returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.invites("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/{team}/invites returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.invites("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/invite returns successfully", async () => {
      const result = await client.v1.teams.invite("test-value", { team: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/{team}/invite returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.invite("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/{team}/invite returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.invite("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/teams/{team}/invite returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.invite("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("teams > artifacts", () => {
    it("GET /api/v1/teams/{team}/artifacts returns successfully", async () => {
      const result = await client.v1.teams.artifacts.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/teams/{team}/artifacts returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.artifacts.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/teams/{team}/artifacts returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.artifacts.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/artifacts returns successfully", async () => {
      const result = await client.v1.teams.artifacts.create("test-value", { artifact: {} });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/{team}/artifacts returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.artifacts.create("test-value", { artifact: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/{team}/artifacts returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.teams.artifacts.create("test-value", { artifact: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/teams/{team}/artifacts returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.teams.artifacts.create("test-value", { artifact: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("teams > custom_objects", () => {
    it("GET /api/v1/teams/{team}/custom_objects returns successfully", async () => {
      const result = await client.v1.teams.custom_objects.list("test-value", { type: "test" });
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/teams/{team}/custom_objects returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.custom_objects.list("test-value", { type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/teams/{team}/custom_objects returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.custom_objects.list("test-value", { type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/custom_objects returns successfully", async () => {
      const result = await client.v1.teams.custom_objects.create("test-value", { fields: {}, team: "test-value", type: "test" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/{team}/custom_objects returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.custom_objects.create("test-value", { fields: {}, team: "test-value", type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/{team}/custom_objects returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.custom_objects.create("test-value", { fields: {}, team: "test-value", type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/custom_objects returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.teams.custom_objects.create("test-value", { fields: {}, team: "test-value", type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("teams > members", () => {
    it("GET /api/v1/teams/{team}/members returns successfully", async () => {
      const result = await client.v1.teams.members.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/teams/{team}/members returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.members.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/teams/{team}/members returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.members.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/members returns successfully", async () => {
      const result = await client.v1.teams.members.create("test-value", { team: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/{team}/members returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.members.create("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/{team}/members returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.members.create("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/members returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.teams.members.create("test-value", { team: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("teams > threads", () => {
    it("GET /api/v1/teams/{team}/threads returns successfully", async () => {
      const result = await client.v1.teams.threads.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/teams/{team}/threads returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.threads.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/teams/{team}/threads returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.threads.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/threads returns successfully", async () => {
      const result = await client.v1.teams.threads.create("test-value", { team: "test-value", thread: {} });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/teams/{team}/threads returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.teams.threads.create("test-value", { team: "test-value", thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/teams/{team}/threads returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.teams.threads.create("test-value", { team: "test-value", thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/teams/{team}/threads returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.teams.threads.create("test-value", { team: "test-value", thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
