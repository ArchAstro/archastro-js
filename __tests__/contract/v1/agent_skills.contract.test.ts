// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 75bf4cd8cc8c


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: agent_skills", () => {
  describe("agent_skills", () => {
    it("POST /api/v1/agent_skills/{agent_skill}/activate returns successfully", async () => {
      const result = await client.v1.agent_skills.activate("test-value", { agent_skill: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_skills/{agent_skill}/activate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_skills.activate("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_skills/{agent_skill}/activate returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_skills.activate("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_skills/{agent_skill}/activate returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_skills.activate("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/agent_skills/{agent_skill} returns successfully", async () => {
      const result = await client.v1.agent_skills.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/agent_skills/{agent_skill} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_skills.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/agent_skills/{agent_skill} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_skills.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/agent_skills/{agent_skill} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_skills.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agent_skills/{agent_skill} returns successfully", async () => {
      const result = await client.v1.agent_skills.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_skills/{agent_skill} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_skills.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_skills/{agent_skill} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_skills.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_skills/{agent_skill} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_skills.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agent_skills/{agent_skill} returns successfully", async () => {
      const result = await client.v1.agent_skills.update("test-value", { agent_skill: "test-value" });
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/agent_skills/{agent_skill} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_skills.update("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/agent_skills/{agent_skill} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_skills.update("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PATCH /api/v1/agent_skills/{agent_skill} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_skills.update("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agent_skills/{agent_skill} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_skills.update("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/agent_skills returns successfully", async () => {
      const result = await client.v1.agent_skills.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_skills returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_skills.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_skills returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_skills.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_skills returns successfully", async () => {
      const result = await client.v1.agent_skills.create({ agent: "test-value", config: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_skills returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_skills.create({ agent: "test-value", config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_skills returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_skills.create({ agent: "test-value", config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_skills returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_skills.create({ agent: "test-value", config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_skills returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_skills.create({ agent: "test-value", config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_skills/{agent_skill}/deactivate returns successfully", async () => {
      const result = await client.v1.agent_skills.deactivate("test-value", { agent_skill: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_skills/{agent_skill}/deactivate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_skills.deactivate("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_skills/{agent_skill}/deactivate returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_skills.deactivate("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_skills/{agent_skill}/deactivate returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_skills.deactivate("test-value", { agent_skill: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

});
