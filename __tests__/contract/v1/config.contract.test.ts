// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: b25d9ee76ffc


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: config", () => {
  describe("config", () => {
    it("DELETE /api/v1/config/{config} returns successfully", async () => {
      await client.v1.config.delete("test-value");
    });

    it("DELETE /api/v1/config/{config} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("DELETE /api/v1/config/{config} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/config/{config} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/config/{config} returns successfully", async () => {
      const result = await client.v1.config.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/config/{config} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/config/{config} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/config/{config} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/config/{config} returns successfully", async () => {
      const result = await client.v1.config.replace("test-value", { config: "test-value", mime_type: "application/json", raw_content: "test content" });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/config/{config} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.replace("test-value", { config: "test-value", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("PUT /api/v1/config/{config} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.replace("test-value", { config: "test-value", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/config/{config} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.replace("test-value", { config: "test-value", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/config/{config} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.config.replace("test-value", { config: "test-value", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/config/validate returns successfully", async () => {
      const result = await client.v1.config.validate({ kind: "test", mime_type: "application/json", raw_content: "test content" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/config/validate returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.validate({ kind: "test", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/config/validate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.validate({ kind: "test", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/config returns successfully", async () => {
      const result = await client.v1.config.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/config returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/config returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/config returns successfully", async () => {
      const result = await client.v1.config.create({ kind: "test", mime_type: "application/json", raw_content: "test content" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/config returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.create({ kind: "test", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/config returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.create({ kind: "test", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/config returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.config.create({ kind: "test", mime_type: "application/json", raw_content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/config/{config}/unarchive returns successfully", async () => {
      const result = await client.v1.config.unarchive("test-value", { config: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/config/{config}/unarchive returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.unarchive("test-value", { config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/config/{config}/unarchive returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.unarchive("test-value", { config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/config/{config}/unarchive returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.unarchive("test-value", { config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/config/{config}/archive returns successfully", async () => {
      const result = await client.v1.config.archive("test-value", { config: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/config/{config}/archive returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.archive("test-value", { config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/config/{config}/archive returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.archive("test-value", { config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/config/{config}/archive returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.archive("test-value", { config: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/config/{config}/versions returns successfully", async () => {
      const result = await client.v1.config.versions("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/config/{config}/versions returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.versions("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/config/{config}/versions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.versions("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/config/{config}/versions returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.versions("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/config/encrypt_secret returns successfully", async () => {
      const result = await client.v1.config.encryptSecret({ plaintext: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/config/encrypt_secret returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.encryptSecret({ plaintext: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/config/encrypt_secret returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.config.encryptSecret({ plaintext: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/config/{config}/content returns successfully", async () => {
      const result = await client.v1.config.content("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/config/{config}/content returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/config/{config}/content returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/config/{config}/content returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("config > kinds", () => {
    it("GET /api/v1/config/kinds returns successfully", async () => {
      const result = await client.v1.config.kinds.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/config/kinds returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.kinds.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/config/kinds/{kind}/schema returns successfully", async () => {
      const result = await client.v1.config.kinds.schema("test");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/config/kinds/{kind}/schema returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.kinds.schema("test");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/config/kinds/{kind}/schema returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.kinds.schema("test");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("config > system", () => {
    it("GET /api/v1/config/system returns successfully", async () => {
      const result = await client.v1.config.system.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/config/system returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.system.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/config/system/{system}/clone returns successfully", async () => {
      const result = await client.v1.config.system.clone("test-value", { system: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/config/system/{system}/clone returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.config.system.clone("test-value", { system: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/config/system/{system}/clone returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.system.clone("test-value", { system: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/config/system/{system}/clone returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.system.clone("test-value", { system: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/config/system/{system}/clone returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.config.system.clone("test-value", { system: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/config/system/{system} returns successfully", async () => {
      const result = await client.v1.config.system.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/config/system/{system} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.config.system.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/config/system/{system} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.config.system.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

});
