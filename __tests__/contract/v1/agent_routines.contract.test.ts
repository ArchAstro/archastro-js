// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: fe4f32e4908b


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: agent_routines", () => {
  describe("agent_routines", () => {
    it("GET /api/v1/agent_routines returns successfully", async () => {
      const result = await client.v1.agent_routines.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/agent_routines returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_routines returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/invoke returns successfully", async () => {
      const result = await client.v1.agent_routines.invoke("test-value", { message: "test-value", routine: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_routines/{routine}/invoke returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.invoke("test-value", { message: "test-value", routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/invoke returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.invoke("test-value", { message: "test-value", routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/invoke returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.invoke("test-value", { message: "test-value", routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/invoke returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_routines.invoke("test-value", { message: "test-value", routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/activate returns successfully", async () => {
      const result = await client.v1.agent_routines.activate("test-value", { routine: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_routines/{routine}/activate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.activate("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/activate returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.activate("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/activate returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.activate("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/activate returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_routines.activate("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/pause returns successfully", async () => {
      const result = await client.v1.agent_routines.pause("test-value", { routine: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_routines/{routine}/pause returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.pause("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/pause returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.pause("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_routines/{routine}/pause returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.pause("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/agent_routines/{routine} returns successfully", async () => {
      await client.v1.agent_routines.delete("test-value");
    });

    it("DELETE /api/v1/agent_routines/{routine} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/agent_routines/{routine} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/agent_routines/{routine} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agent_routines/{routine} returns successfully", async () => {
      const result = await client.v1.agent_routines.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_routines/{routine} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_routines/{routine} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_routines/{routine} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agent_routines/{routine} returns successfully", async () => {
      const result = await client.v1.agent_routines.update("test-value", { routine: "test-value" });
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/agent_routines/{routine} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.update("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/agent_routines/{routine} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.update("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PATCH /api/v1/agent_routines/{routine} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.update("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agent_routines/{routine} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_routines.update("test-value", { routine: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("agent_routines > runs", () => {
    it("GET /api/v1/agent_routines/{routine}/runs returns successfully", async () => {
      const result = await client.v1.agent_routines.runs.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/agent_routines/{routine}/runs returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.agent_routines.runs.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/agent_routines/{routine}/runs returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.runs.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_routines/{routine}/runs returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.runs.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_routines/{routine}/runs returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.runs.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agent_routines/runs/{run} returns successfully", async () => {
      const result = await client.v1.agent_routines.runs.get("test-value", "test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_routines/runs/{run} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.runs.get("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_routines/runs/{run} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.runs.get("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_routines/runs/{run} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_routines.runs.get("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("agent_routines > presets", () => {
    it("GET /api/v1/agent_routines/presets returns successfully", async () => {
      const result = await client.v1.agent_routines.presets.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_routines/presets returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_routines.presets.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_routines/presets returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_routines.presets.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

  });

});
