// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: c00d199201f8


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: users", () => {
  describe("users", () => {
    it("GET /api/v1/users/{user} returns successfully", async () => {
      const result = await client.v1.users.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/users/{user} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/users/{user} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.users.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

  });

  describe("users > me", () => {
    it("GET /api/v1/users/me returns successfully", async () => {
      const result = await client.v1.users.me.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/users/me returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.me.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

  describe("users > orgs", () => {
    it("GET /api/v1/users/{user}/orgs returns successfully", async () => {
      const result = await client.v1.users.orgs.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/users/{user}/orgs returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.orgs.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

  describe("users > artifacts", () => {
    it("GET /api/v1/users/{user}/artifacts returns successfully", async () => {
      const result = await client.v1.users.artifacts.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/users/{user}/artifacts returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.artifacts.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/users/{user}/artifacts returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.users.artifacts.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/users/{user}/artifacts returns successfully", async () => {
      const result = await client.v1.users.artifacts.create("test-value", { artifact: {} });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/users/{user}/artifacts returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.artifacts.create("test-value", { artifact: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/users/{user}/artifacts returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.users.artifacts.create("test-value", { artifact: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/users/{user}/artifacts returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.users.artifacts.create("test-value", { artifact: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("users > threads", () => {
    it("GET /api/v1/users/{user}/threads returns successfully", async () => {
      const result = await client.v1.users.threads.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/users/{user}/threads returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.threads.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/users/{user}/threads returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.users.threads.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/users/{user}/threads returns successfully", async () => {
      const result = await client.v1.users.threads.create("test-value", { thread: {}, user: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/users/{user}/threads returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.threads.create("test-value", { thread: {}, user: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/users/{user}/threads returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.users.threads.create("test-value", { thread: {}, user: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/users/{user}/threads returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.users.threads.create("test-value", { thread: {}, user: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("users > profile", () => {
    it("PUT /api/v1/users/{user}/profile returns successfully", async () => {
      const result = await client.v1.users.profile.replace("test-value", { user: "test-value" });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/users/{user}/profile returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.users.profile.replace("test-value", { user: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/users/{user}/profile returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.users.profile.replace("test-value", { user: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
