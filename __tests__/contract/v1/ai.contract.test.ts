// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 9aeb2a42036a


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: ai", () => {
  describe("ai > chat", () => {
    it("POST /api/v1/ai/chat/completions returns successfully", async () => {
      const result = await client.v1.ai.chat.completions({ messages: [{ role: "user" }], opts: { model: "test-model" } });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/ai/chat/completions returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.ai.chat.completions({ messages: [{ role: "user" }], opts: { model: "test-model" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/ai/chat/completions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.ai.chat.completions({ messages: [{ role: "user" }], opts: { model: "test-model" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/ai/chat/completions returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.ai.chat.completions({ messages: [{ role: "user" }], opts: { model: "test-model" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("ai > chat > models", () => {
    it("GET /api/v1/ai/chat/models returns successfully", async () => {
      const result = await client.v1.ai.chat.models.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/ai/chat/models returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.ai.chat.models.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

  describe("ai > image", () => {
    it("POST /api/v1/ai/image/edits returns successfully", async () => {
      const result = await client.v1.ai.image.edits({ images: [{ image_data: "test-value", image_type: "test-value" }], prompt: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/ai/image/edits returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.ai.image.edits({ images: [{ image_data: "test-value", image_type: "test-value" }], prompt: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/ai/image/edits returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.ai.image.edits({ images: [{ image_data: "test-value", image_type: "test-value" }], prompt: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/ai/image/edits returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.ai.image.edits({ images: [{ image_data: "test-value", image_type: "test-value" }], prompt: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/ai/image/generations returns successfully", async () => {
      const result = await client.v1.ai.image.generations({ prompt: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/ai/image/generations returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.ai.image.generations({ prompt: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/ai/image/generations returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.ai.image.generations({ prompt: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/ai/image/generations returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.ai.image.generations({ prompt: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("ai > image > models", () => {
    it("GET /api/v1/ai/image/models returns successfully", async () => {
      const result = await client.v1.ai.image.models.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/ai/image/models returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.ai.image.models.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

});
