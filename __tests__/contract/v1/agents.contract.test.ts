// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 3b4d39d9e189


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: agents", () => {
  describe("agents", () => {
    it("DELETE /api/v1/agents/{agent} returns successfully", async () => {
      await client.v1.agents.delete("test-value");
    });

    it("DELETE /api/v1/agents/{agent} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/agents/{agent} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/agents/{agent} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agents/{agent} returns successfully", async () => {
      const result = await client.v1.agents.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agents/{agent} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agents/{agent} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agents/{agent} returns successfully", async () => {
      const result = await client.v1.agents.update("test-value", { agent: "test-value" });
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/agents/{agent} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.update("test-value", { agent: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/agents/{agent} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.update("test-value", { agent: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PATCH /api/v1/agents/{agent} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.update("test-value", { agent: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agents/{agent} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.update("test-value", { agent: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_routines returns successfully", async () => {
      const result = await client.v1.agents.agentRoutines("test-value", { agent: "test-value", handler_type: "test-value", name: "test-name" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agents/{agent}/agent_routines returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agentRoutines("test-value", { agent: "test-value", handler_type: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_routines returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agentRoutines("test-value", { agent: "test-value", handler_type: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_routines returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.agentRoutines("test-value", { agent: "test-value", handler_type: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_routines returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.agentRoutines("test-value", { agent: "test-value", handler_type: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/agents returns successfully", async () => {
      const result = await client.v1.agents.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/agents returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents returns successfully", async () => {
      const result = await client.v1.agents.create({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agents returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.agents.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/agents returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agents returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agents/{agent}/threads returns successfully", async () => {
      const result = await client.v1.agents.threads("test-value", { agent: "test-value", thread: {} });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agents/{agent}/threads returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.threads("test-value", { agent: "test-value", thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agents/{agent}/threads returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.threads("test-value", { agent: "test-value", thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents/{agent}/threads returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.threads("test-value", { agent: "test-value", thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/threads returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.threads("test-value", { agent: "test-value", thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agents/{agent}/search returns successfully", async () => {
      const result = await client.v1.agents.search("test-value", { agent: "test-value", query: "test-value" });
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("POST /api/v1/agents/{agent}/search returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.search("test-value", { agent: "test-value", query: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agents/{agent}/search returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.search("test-value", { agent: "test-value", query: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/search returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.search("test-value", { agent: "test-value", query: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("agents > agent_installations", () => {
    it("GET /api/v1/agents/{agent}/agent_installations returns successfully", async () => {
      const result = await client.v1.agents.agent_installations.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/agents/{agent}/agent_installations returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_installations.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_installations returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_installations.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_installations returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.agent_installations.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_installations returns successfully", async () => {
      const result = await client.v1.agents.agent_installations.create("test-value", { agent: "test-value", kind: "test" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agents/{agent}/agent_installations returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_installations.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_installations returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_installations.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_installations returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.agent_installations.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_installations returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.agent_installations.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("agents > agent_installations > kinds", () => {
    it("GET /api/v1/agents/{agent}/agent_installations/kinds returns successfully", async () => {
      const result = await client.v1.agents.agent_installations.kinds.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/agents/{agent}/agent_installations/kinds returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_installations.kinds.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_installations/kinds returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_installations.kinds.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

  });

  describe("agents > agent_computers", () => {
    it("GET /api/v1/agents/{agent}/agent_computers returns successfully", async () => {
      const result = await client.v1.agents.agent_computers.list("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agents/{agent}/agent_computers returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_computers.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_computers returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_computers.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_computers returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.agent_computers.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_computers returns successfully", async () => {
      const result = await client.v1.agents.agent_computers.create("test-value", { agent: "test-value", name: "test-name" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agents/{agent}/agent_computers returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_computers.create("test-value", { agent: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_computers returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_computers.create("test-value", { agent: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_computers returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.agent_computers.create("test-value", { agent: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_computers returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.agent_computers.create("test-value", { agent: "test-value", name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("agents > agent_tools", () => {
    it("GET /api/v1/agents/{agent}/agent_tools returns successfully", async () => {
      const result = await client.v1.agents.agent_tools.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/agents/{agent}/agent_tools returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_tools.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_tools returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_tools.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_tools returns successfully", async () => {
      const result = await client.v1.agents.agent_tools.create("test-value", { agent: "test-value", kind: "test" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agents/{agent}/agent_tools returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_tools.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_tools returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_tools.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_tools returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.agent_tools.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agents/{agent}/agent_tools returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agents.agent_tools.create("test-value", { agent: "test-value", kind: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("agents > schedules", () => {
    it("GET /api/v1/agents/{agent}/schedules/{schedule} returns successfully", async () => {
      const result = await client.v1.agents.schedules.get("test-value", "test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agents/{agent}/schedules/{schedule} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.schedules.get("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent}/schedules/{schedule} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.schedules.get("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agents/{agent}/schedules/{schedule} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.schedules.get("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agents/{agent}/schedules returns successfully", async () => {
      const result = await client.v1.agents.schedules.list("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agents/{agent}/schedules returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.agents.schedules.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/agents/{agent}/schedules returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.schedules.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent}/schedules returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.schedules.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

  });

  describe("agents > agent_working_memory", () => {
    it("GET /api/v1/agents/{agent}/agent_working_memory returns successfully", async () => {
      const result = await client.v1.agents.agent_working_memory.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/agents/{agent}/agent_working_memory returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agents.agent_working_memory.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_working_memory returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agents.agent_working_memory.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agents/{agent}/agent_working_memory returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agents.agent_working_memory.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

});
