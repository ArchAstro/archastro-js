// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 5c502f6d7d15


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: threads", () => {
  describe("threads", () => {
    it("POST /api/v1/threads/{thread}/mark_read returns successfully", async () => {
      const result = await client.v1.threads.markRead("test-value", { thread: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/threads/{thread}/mark_read returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.markRead("test-value", { thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/threads/{thread}/mark_read returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.markRead("test-value", { thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/threads/{thread}/mark_read returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.threads.markRead("test-value", { thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/threads/{thread} returns successfully", async () => {
      await client.v1.threads.delete("test-value");
    });

    it("DELETE /api/v1/threads/{thread} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/threads/{thread} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/threads/{thread} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/threads/{thread} returns successfully", async () => {
      const result = await client.v1.threads.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/threads/{thread} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/threads/{thread} returns successfully", async () => {
      const result = await client.v1.threads.replace("test-value", { thread: "test-value" });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/threads/{thread} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.replace("test-value", { thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/threads/{thread} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.replace("test-value", { thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PUT /api/v1/threads/{thread} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.replace("test-value", { thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/threads/{thread} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.threads.replace("test-value", { thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("threads > read_status", () => {
    it("GET /api/v1/threads/{thread}/read_status returns successfully", async () => {
      const result = await client.v1.threads.read_status.list("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/threads/{thread}/read_status returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.read_status.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread}/read_status returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.read_status.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread}/read_status returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.read_status.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("threads > search", () => {
    it("GET /api/v1/threads/{thread}/search returns successfully", async () => {
      const result = await client.v1.threads.search.list("test-value", { q: "test-value" });
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/threads/{thread}/search returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.search.list("test-value", { q: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread}/search returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.search.list("test-value", { q: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread}/search returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.search.list("test-value", { q: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("threads > messages", () => {
    it("GET /api/v1/threads/{thread}/messages returns successfully", async () => {
      const result = await client.v1.threads.messages.list("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/threads/{thread}/messages returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.messages.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread}/messages returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.messages.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread}/messages returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.messages.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("threads > members", () => {
    it("DELETE /api/v1/threads/{thread}/members returns successfully", async () => {
      await client.v1.threads.members.remove("test-value");
    });

    it("DELETE /api/v1/threads/{thread}/members returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.members.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/threads/{thread}/members returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.members.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/threads/{thread}/members returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.threads.members.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/threads/{thread}/members returns successfully", async () => {
      const result = await client.v1.threads.members.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/threads/{thread}/members returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.members.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread}/members returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.members.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread}/members returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.members.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/threads/{thread}/members returns successfully", async () => {
      const result = await client.v1.threads.members.create("test-value", { thread: "test-value", type: "test" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/threads/{thread}/members returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.members.create("test-value", { thread: "test-value", type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/threads/{thread}/members returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.members.create("test-value", { thread: "test-value", type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/threads/{thread}/members returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.threads.members.create("test-value", { thread: "test-value", type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("threads > picture", () => {
    it("PUT /api/v1/threads/{thread}/picture returns successfully", async () => {
      const result = await client.v1.threads.picture.replace("test-value", { picture: { data: "test-value", filename: "test-value", mime_type: "application/json" }, thread: "test-value" });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/threads/{thread}/picture returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.picture.replace("test-value", { picture: { data: "test-value", filename: "test-value", mime_type: "application/json" }, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/threads/{thread}/picture returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.picture.replace("test-value", { picture: { data: "test-value", filename: "test-value", mime_type: "application/json" }, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PUT /api/v1/threads/{thread}/picture returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.picture.replace("test-value", { picture: { data: "test-value", filename: "test-value", mime_type: "application/json" }, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/threads/{thread}/picture returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.threads.picture.replace("test-value", { picture: { data: "test-value", filename: "test-value", mime_type: "application/json" }, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("threads > agents", () => {
    it("GET /api/v1/threads/{thread}/agents returns successfully", async () => {
      const result = await client.v1.threads.agents.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/threads/{thread}/agents returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.agents.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread}/agents returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.agents.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread}/agents returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.agents.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("threads > settings", () => {
    it("GET /api/v1/threads/{thread}/settings returns successfully", async () => {
      const result = await client.v1.threads.settings.list("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/threads/{thread}/settings returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.settings.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread}/settings returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.settings.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread}/settings returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.settings.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/threads/{thread}/settings returns successfully", async () => {
      const result = await client.v1.threads.settings.replace("test-value", { settings: {}, thread: "test-value" });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/threads/{thread}/settings returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.settings.replace("test-value", { settings: {}, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/threads/{thread}/settings returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.settings.replace("test-value", { settings: {}, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PUT /api/v1/threads/{thread}/settings returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.settings.replace("test-value", { settings: {}, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/threads/{thread}/settings returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.threads.settings.replace("test-value", { settings: {}, thread: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("threads > artifacts", () => {
    it("GET /api/v1/threads/{thread}/artifacts returns successfully", async () => {
      const result = await client.v1.threads.artifacts.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/threads/{thread}/artifacts returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.threads.artifacts.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/threads/{thread}/artifacts returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.threads.artifacts.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/threads/{thread}/artifacts returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.threads.artifacts.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

});
