// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: f129b1658f35

import { HttpClient } from "../../runtime/http-client.js";
import type { CustomObject } from "../../types/common.js";
import type { Task } from "../../types/tasks.js";
import type { Team, TeamInvite, TeamMembership } from "../../types/teams.js";
import type { Thread } from "../../types/threads.js";

export class TeamCustomObjectResource {
  constructor(private http: HttpClient) {}

  /**
   * List a team's custom objects
   * Returns a paginated list of custom objects owned by the specified team,
   * filtered to a single schema type. Results are ordered by creation time
   * descending unless `query` is provided, in which case they are ordered by
   * full-text relevance score descending.
   * Use `limit` and `offset` for page-based pagination. Use `row_key` or
   * `sort_key` to narrow results to objects matching those index values.
   * Full-text search via `query` operates only against the fields configured
   * as `search_fields` on the schema.
   * The authenticated user must be a member of the team with sufficient
   * access. Returns 404 if the team is not found, the caller lacks access,
   * or `type` does not match a registered schema for the team's organization.
   * @param team - Team ID (`team_...`). Scopes results to objects owned by this team.
   * @param params - Query parameters.
   * @param params.type - Schema type identifier (`lookup_key`) that filters results to objects of this schema.
   * @param params.limit - Maximum number of objects to return per page.
   * @param params.offset - Number of objects to skip before returning results. Use with `limit` for page-based pagination.
   * @param params.rowKey - Filter results to objects whose `row_key` exactly matches this value.
   * @param params.sortKey - Filter results to objects whose `sort_key` exactly matches this value.
   * @param params.query - Full-text search string matched against the schema's configured `search_fields`. When provided, results are ordered by relevance score descending instead of creation time descending.
   * @returns Successful response
   */
  async list(team: string, params?: { type?: string; limit?: number; offset?: number; rowKey?: string; sortKey?: string; query?: string }): Promise<{ data: { acl?: { add?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; grants?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; remove?: { principal?: string | undefined; principal_type: string }[] | undefined } | undefined; created_at?: string | undefined; fields?: Record<string, unknown> | undefined; id: string; org?: string | undefined; row_key?: string | undefined; sandbox?: string | undefined; schema_type?: string | undefined; team?: string | undefined; updated_at?: string | undefined; user?: string | undefined; version?: number | undefined }[]; meta?: Record<string, unknown> | undefined }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    query["type"] = params?.type;
    if (params?.limit !== undefined) {
      query["limit"] = params?.limit;
    }
    if (params?.offset !== undefined) {
      query["offset"] = params?.offset;
    }
    if (params?.rowKey !== undefined) {
      query["row_key"] = params?.rowKey;
    }
    if (params?.sortKey !== undefined) {
      query["sort_key"] = params?.sortKey;
    }
    if (params?.query !== undefined) {
      query["query"] = params?.query;
    }

    return this.http.request(`/api/v1/teams/${team}/custom_objects`, { query });
  }

  /**
   * Create a team custom object
   * Creates a new custom object owned by the specified team. The object is
   * instantiated against the schema identified by `type` (the schema's
   * `lookup_key`). All field values are validated against that schema's
   * field definitions before the object is persisted.
   * The authenticated user must be a member of the team with sufficient
   * access. If the team is not found or the caller lacks access, the endpoint
   * returns 404. If `type` does not match a registered schema for the team's
   * organization, the endpoint also returns 404.
   * @param team - Team ID (`team_...`). Scopes results to objects owned by this team.
   * @param input - Request body.
   * @param input.fields - Map of field values to set on the new object. Keys and value types must conform to the schema identified by `type`.
   * @param input.type - Schema type identifier (`lookup_key`) that defines the object's fields and validation rules.
   * @returns The newly created custom object.
   */
  async create(team: string, input: { fields: Record<string, unknown>; type: string }): Promise<CustomObject> {
    return this.http.request(`/api/v1/teams/${team}/custom_objects`, { method: "POST", body: input });
  }
}

export class MemberResource {
  constructor(private http: HttpClient) {}

  /**
   * Remove a member or org from a team
   * Removes a user, agent, or all members of an organization from the specified
   * team. Provide exactly one of `user`, `agent`, or `org` — supplying more than
   * one or none returns a 400 error. On success, returns 204 No Content.
   * When `org` is provided, every user and agent membership belonging to that org
   * is removed in a single call. The caller must be a member of the team's owning
   * org to perform an org-scoped removal. You cannot target the team's owning org
   * itself with this parameter.
   * The caller must have permission to manage the team. When `app` is present, the
   * request is scoped to that app and requires a valid app-scoped token.
   * @param team - Team ID (`team_...`). The team to remove the member from.
   * @returns Empty response. Returns 204 No Content on success.
   */
  async remove(team: string): Promise<void> {
    await this.http.request(`/api/v1/teams/${team}/members`, { method: "DELETE" });
  }

  /**
   * List members of a team
   * Returns all members of the specified team, including both users and agents.
   * Members are returned in a single non-paginated array ordered by join time.
   * Bearer-authenticated users must be a member of the team to retrieve its
   * member list. Developer and server-to-server callers can retrieve members for
   * any team visible to their app scope. When `app` is provided, the request is
   * scoped to that app and requires a valid app-scoped token.
   * @param team - Team ID (`team_...`). The team to remove the member from.
   * @returns Successful response
   */
  async list(team: string): Promise<{ data: { agent?: { acl?: { add?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; grants?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; remove?: { principal?: string | undefined; principal_type: string }[] | undefined } | null | undefined; app?: string | undefined; created_at?: string | undefined; default_model?: string | null | undefined; description?: string | null | undefined; email?: string | null | undefined; id: string; identity?: string | null | undefined; last_applied_template_config?: string | null | undefined; lookup_key?: string | null | undefined; metadata?: Record<string, unknown> | undefined; name?: string | undefined; org?: string | null | undefined; org_name?: string | null | undefined; originator?: string | null | undefined; phone_number?: string | null | undefined; sandbox?: string | null | undefined; source_solution?: { current_solution?: { category_keys?: string[] | undefined; created_at?: string | undefined; description?: string | null | undefined; events?: Record<string, unknown> | undefined; id: string; image_url?: string | null | undefined; kind: string; latest_solution?: string | null | undefined; latest_version?: string | null | undefined; lookup_key?: string | null | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_logo?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined; org_name?: string | null | undefined; org_slug?: string | null | undefined; owners: string[]; readme_url?: string | null | undefined; screenshot_urls?: string[] | undefined; solution_id?: string | null | undefined; solution_version?: string | null | undefined; tag_keys?: string[] | undefined; template_kind?: string | null | undefined; templates: { description?: string | null | undefined; details?: { automation_type?: string | null | undefined; invoke_contract?: { input_schema?: Record<string, unknown> | null | undefined; participants?: { description?: string | null | undefined; name: string; required: boolean; type: string }[] | null | undefined; prefills: { participants?: Record<string, unknown> | undefined; payload?: Record<string, unknown> | undefined } } | null | undefined; type: "automation" } | null | undefined; display_name?: string | null | undefined; id?: string | null | undefined; kind: string; lookup_key?: string | null | undefined; name?: string | null | undefined; readme_url?: string | null | undefined; virtual_path?: string | null | undefined }[]; updated_at?: string | undefined; upgrade_available: boolean; virtual_path?: string | null | undefined } | undefined; solution: { category_keys?: string[] | undefined; created_at?: string | undefined; description?: string | null | undefined; events?: Record<string, unknown> | undefined; id: string; image_url?: string | null | undefined; kind: string; latest_solution?: string | null | undefined; latest_version?: string | null | undefined; lookup_key?: string | null | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_logo?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined; org_name?: string | null | undefined; org_slug?: string | null | undefined; owners: string[]; readme_url?: string | null | undefined; screenshot_urls?: string[] | undefined; solution_id?: string | null | undefined; solution_version?: string | null | undefined; tag_keys?: string[] | undefined; template_kind?: string | null | undefined; templates: { description?: string | null | undefined; details?: { automation_type?: string | null | undefined; invoke_contract?: { input_schema?: Record<string, unknown> | null | undefined; participants?: { description?: string | null | undefined; name: string; required: boolean; type: string }[] | null | undefined; prefills: { participants?: Record<string, unknown> | undefined; payload?: Record<string, unknown> | undefined } } | null | undefined; type: "automation" } | null | undefined; display_name?: string | null | undefined; id?: string | null | undefined; kind: string; lookup_key?: string | null | undefined; name?: string | null | undefined; readme_url?: string | null | undefined; virtual_path?: string | null | undefined }[]; updated_at?: string | undefined; upgrade_available: boolean; virtual_path?: string | null | undefined }; template: { created_at?: string | undefined; description?: string | null | undefined; display_name?: string | null | undefined; id: string; kind: string; lookup_key?: string | null | undefined; name?: string | null | undefined; updated_at?: string | undefined; virtual_path?: string | null | undefined } } | null | undefined; team?: string | null | undefined; template_upgrade_available?: boolean | null | undefined; updated_at?: string | undefined; user?: string | null | undefined } | null | undefined; created_at?: string | undefined; id: string; joined_at?: string | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined; role?: string | undefined; team?: Record<string, unknown> | null | undefined; type?: string | undefined; updated_at?: string | undefined; user?: { alias?: string | null | undefined; app?: string | null | undefined; app_name?: string | null | undefined; created_by_agent_user?: string | null | undefined; created_by_developer?: string | null | undefined; created_by_org?: string | null | undefined; created_by_team?: string | null | undefined; created_by_user?: string | null | undefined; email?: string | null | undefined; id: string; is_system_user?: boolean | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_name?: string | null | undefined; org_role?: string | null | undefined; org_slug?: string | null | undefined; sandbox?: string | null | undefined; sandbox_name?: string | null | undefined } | null | undefined }[] }> {
    return this.http.request(`/api/v1/teams/${team}/members`);
  }

  /**
   * Add a member to a team
   * Adds a user or agent as a member of the specified team and returns the new
   * membership with HTTP 201. Provide exactly one of `user` or `agent` — supplying
   * both or neither returns a 400 error.
   * Adding a user requires permission to manage the team (team owner, team
   * admin, or org admin). Adding an agent with the default `"member"` role is
   * also open to any existing member of the team; assigning an elevated role to
   * an agent still requires team-manage permission. When an `app` is provided,
   * the request is scoped to that app and the caller must hold a valid app-scoped
   * token. The default role is `"member"` when `role` is omitted.
   * @param team - Team ID (`team_...`). The team to remove the member from.
   * @param input - Request body.
   * @param input.agent - Agent ID (`agt_...`) to add as a member. Provide exactly one of `user` or `agent`.
   * @param input.role - Role to assign. One of `"owner"`, `"admin"`, or `"member"`. Defaults to `"member"` when omitted.
   * @param input.user - User ID (`usr_...`) to add as a member. Provide exactly one of `user` or `agent`.
   * @returns The newly created team membership.
   */
  async create(team: string, input: { agent?: string | undefined; role?: string | undefined; user?: string | undefined }): Promise<TeamMembership> {
    return this.http.request(`/api/v1/teams/${team}/members`, { method: "POST", body: input });
  }

  /**
   * Update a team member's role
   * Changes the role of an existing user member on the specified team. Returns the
   * updated membership on success.
   * Only user memberships are supported by this endpoint. Attempting to update an
   * agent membership returns 404. To change an agent's role, remove the existing
   * membership and re-add the agent with the desired role.
   * The caller must have permission to modify the team. You cannot change a member's
   * role across organization boundaries. Demoting the last owner of a team returns
   * 409. An invalid `role` value returns 422. When `app` is provided, the request
   * is scoped to that app and requires a valid app-scoped token.
   * @param team - Team ID (`team_...`). The team to remove the member from.
   * @param user - User ID (`usr_...`) of the existing member whose role should be changed.
   * @param input - Request body.
   * @param input.role - New role to assign. One of `"owner"`, `"admin"`, or `"member"`.
   * @returns The updated team membership reflecting the new role.
   */
  async update(team: string, user: string, input: { role: string }): Promise<TeamMembership> {
    return this.http.request(`/api/v1/teams/${team}/members/${user}`, { method: "PATCH", body: input });
  }
}

export class TeamTaskResource {
  constructor(private http: HttpClient) {}

  /**
   * List an owner's tasks
   * Returns tasks owned by the specified user or team. You can narrow results using the
   * optional filters below. By default results are returned in reverse chronological
   * order (most recently created first); use `sort` and `order` to sort by due date or
   * priority instead.
   * User-authenticated callers may list their personal tasks or tasks for teams they
   * have joined. Privileged callers provide the owner in the route; the owner's
   * organization is implied by that principal. An explicit `org` is optional and,
   * when set, must match the owner's organization.
   * @param team - Team ID (`tem_...`). Only tasks belonging to this team are returned.
   * @param params - Query parameters.
   * @param params.user - User ID (`usr_...`) for user-scoped tasks.
   * @param params.org - Optional organization (`org_...`) for developer and server-to-server calls. When omitted, the org is taken from the owner principal (team, user, or agent). When set, it must match that principal's org; pass null for an owner outside an organization.
   * @param params.status - Filter tasks by status. One of `"open"`, `"in_progress"`, or `"done"`. Omit to return tasks in all statuses.
   * @param params.ownerUser - Filter tasks assigned to a specific user. Provide the user's public ID (`usr_...`).
   * @param params.ownerAgent - Filter tasks assigned to a specific agent. Provide the agent's public ID (`agi_...`).
   * @param params.priority - Filter tasks by priority, from 0 (highest) to 4 (lowest).
   * @param params.tag - Return only tasks carrying this tag (matched against the canonical lowercase form).
   * @param params.parent - Return only subtasks of the given task (`tsk_...`), or pass `none` to return only top-level tasks.
   * @param params.sourceScope - Return only tasks whose source matches this container. Must be supplied with `source_type` and `source_id`.
   * @param params.sourceType - Return only tasks whose source matches this object kind. Must be supplied with the other source filters.
   * @param params.sourceId - Return only tasks whose source matches this object identity. Must be supplied with the other source filters.
   * @param params.epic - Return only tasks with this exact epic label.
   * @param params.search - Restrict results to tasks whose name or description contains this string.
   * @param params.sort - Sort key. One of `"created"` (default — most recently created first), `"due_date"` (soonest due first; tasks without a due date always sort last), or `"priority"` (most urgent first). Ties break by most recently created.
   * @param params.order - Sort direction, `"asc"` or `"desc"`. Defaults to `"desc"` for `created` and `"asc"` for `due_date` and `priority`.
   * @param params.dueBefore - Return only tasks with a due date strictly before this ISO 8601 datetime (`2026-08-01T00:00:00Z`) or date (`2026-08-01`, meaning midnight UTC). Tasks without a due date are excluded.
   * @param params.dueAfter - Return only tasks with a due date strictly after this ISO 8601 datetime or date. Tasks without a due date are excluded.
   * @param params.overdue - When `true`, return only overdue tasks: a due date before the current UTC day and a status other than `"done"`. A task due today is not overdue.
   * @param params.ready - When true, return only open tasks with no unfinished blockers and no active session lease. This is a projection snapshot; claim a lease before starting work.
   * @param params.limit - Maximum number of tasks to return. Capped at 100.
   * @param params.afterCursor - Opaque cursor returned by the previous page.
   * @returns Successful response
   */
  async list(team: string, params?: { user?: string; org?: string; status?: string; ownerUser?: string; ownerAgent?: string; priority?: number; tag?: string; parent?: string; sourceScope?: string; sourceType?: string; sourceId?: string; epic?: string; search?: string; sort?: string; order?: string; dueBefore?: string; dueAfter?: string; overdue?: boolean; ready?: boolean; limit?: number; afterCursor?: string }): Promise<{ after_cursor?: string | null | undefined; before_cursor?: string | null | undefined; data: { agent?: string | null | undefined; blocked_by_count?: number | undefined; closed_at?: string | null | undefined; comments_count?: number | undefined; created_at?: string | undefined; created_by_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; created_by_agent?: string | null | undefined; created_by_user?: string | null | undefined; current_lease?: { expires_at: string; harness: string; session_name: string } | null | undefined; description?: string | null | undefined; due_date?: string | null | undefined; epic?: string | null | undefined; id: string; is_blocked?: boolean | undefined; links?: Record<string, unknown> | undefined; metadata?: Record<string, unknown> | undefined; name: string; org?: string | null | undefined; owner_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; owner_agent?: string | null | undefined; owner_user?: string | null | undefined; parent?: string | null | undefined; priority?: number | undefined; sandbox?: string | null | undefined; source_id?: string | null | undefined; source_scope?: string | null | undefined; source_type?: string | null | undefined; status: string; subtasks_count?: number | undefined; tags?: string[] | undefined; team?: string | null | undefined; thread?: string | null | undefined; updated_at?: string | undefined; user?: string | null | undefined }[]; has_more: boolean }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.user !== undefined) {
      query["user"] = params?.user;
    }
    if (params?.org !== undefined) {
      query["org"] = params?.org;
    }
    if (params?.status !== undefined) {
      query["status"] = params?.status;
    }
    if (params?.ownerUser !== undefined) {
      query["owner_user"] = params?.ownerUser;
    }
    if (params?.ownerAgent !== undefined) {
      query["owner_agent"] = params?.ownerAgent;
    }
    if (params?.priority !== undefined) {
      query["priority"] = params?.priority;
    }
    if (params?.tag !== undefined) {
      query["tag"] = params?.tag;
    }
    if (params?.parent !== undefined) {
      query["parent"] = params?.parent;
    }
    if (params?.sourceScope !== undefined) {
      query["source_scope"] = params?.sourceScope;
    }
    if (params?.sourceType !== undefined) {
      query["source_type"] = params?.sourceType;
    }
    if (params?.sourceId !== undefined) {
      query["source_id"] = params?.sourceId;
    }
    if (params?.epic !== undefined) {
      query["epic"] = params?.epic;
    }
    if (params?.search !== undefined) {
      query["search"] = params?.search;
    }
    if (params?.sort !== undefined) {
      query["sort"] = params?.sort;
    }
    if (params?.order !== undefined) {
      query["order"] = params?.order;
    }
    if (params?.dueBefore !== undefined) {
      query["due_before"] = params?.dueBefore;
    }
    if (params?.dueAfter !== undefined) {
      query["due_after"] = params?.dueAfter;
    }
    if (params?.overdue !== undefined) {
      query["overdue"] = params?.overdue;
    }
    if (params?.ready !== undefined) {
      query["ready"] = params?.ready;
    }
    if (params?.limit !== undefined) {
      query["limit"] = params?.limit;
    }
    if (params?.afterCursor !== undefined) {
      query["after_cursor"] = params?.afterCursor;
    }

    return this.http.request(`/api/v1/teams/${team}/tasks`, { query });
  }

  /**
   * Create a task for an owner
   * Creates a new task owned by the specified user or team and returns the full
   * task object. User-authenticated calls are attributed to the authenticated
   * user or agent. App-scoped developer and server-to-server callers must provide
   * the task's explicit `org` scope and an explicit `user` or `agent` actor for
   * team tasks; a user-owned task reuses the user in the route unless an explicit
   * agent is supplied. Every referenced principal is validated against the app,
   * owner, and team membership before creation.
   * @param team - Team ID (`tem_...`). Only tasks belonging to this team are returned.
   * @param input - Request body.
   * @param input.agent - Explicit acting agent (`agi_...`) for a developer or server-to-server call. Mutually exclusive with an acting `user`; the agent must belong to the task owner.
   * @param input.org - Explicit organization (`org_...`) for developer and server-to-server calls. Pass null when the owner is not organization-scoped. The value must match the selected user or team.
   * @param input.task - Attributes for the task to create. `name` is required; all other fields are optional.
   * @param input.user - User ID (`usr_...`). On a user route this is the task owner and creator; on a team route it is the explicit acting user for a developer or server-to-server call.
   * @returns The newly created task.
   */
  async create(team: string, input: { agent?: string | undefined; org?: string | undefined; task: { description?: string | undefined; due_date?: string | undefined; epic?: string | undefined; links?: Record<string, unknown> | undefined; metadata?: Record<string, unknown> | undefined; name: string; owner_agent?: string | undefined; owner_user?: string | undefined; parent?: string | undefined; priority?: number | undefined; source_id?: string | undefined; source_scope?: string | undefined; source_type?: string | undefined; status?: string | undefined; tags?: string[] | undefined; thread?: string | undefined }; user?: string | undefined }): Promise<Task> {
    return this.http.request(`/api/v1/teams/${team}/tasks`, { method: "POST", body: input });
  }

  /**
   * List task blocker cycles
   * Runs an on-demand diagnostic over unfinished tasks owned by the specified
   * team or user and returns a forward cursor-paginated page of complete cyclic
   * blocker components. Detection is bounded to owners with at most 100
   * unfinished tasks. This endpoint is read-only: cycles do not prevent task
   * updates, lease acquisition, or completion.
   * @param team - Team ID (`tem_...`). Only tasks belonging to this team are returned.
   * @param params - Query parameters.
   * @param params.user - User ID (`usr_...`) owning the tasks.
   * @param params.org - Optional organization context for privileged callers.
   * @param params.limit - Maximum cycle components to return. Defaults to 50; maximum is 100.
   * @param params.afterCursor - Opaque cursor returned by the preceding page.
   * @returns Successful response
   */
  async blockerCycles(team: string, params?: { user?: string; org?: string; limit?: number; afterCursor?: string }): Promise<{ after_cursor?: string | undefined; before_cursor?: string | undefined; data: { tasks: { agent?: string | null | undefined; blocked_by_count?: number | undefined; closed_at?: string | null | undefined; comments_count?: number | undefined; created_at?: string | undefined; created_by_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; created_by_agent?: string | null | undefined; created_by_user?: string | null | undefined; current_lease?: { expires_at: string; harness: string; session_name: string } | null | undefined; description?: string | null | undefined; due_date?: string | null | undefined; epic?: string | null | undefined; id: string; is_blocked?: boolean | undefined; links?: Record<string, unknown> | undefined; metadata?: Record<string, unknown> | undefined; name: string; org?: string | null | undefined; owner_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; owner_agent?: string | null | undefined; owner_user?: string | null | undefined; parent?: string | null | undefined; priority?: number | undefined; sandbox?: string | null | undefined; source_id?: string | null | undefined; source_scope?: string | null | undefined; source_type?: string | null | undefined; status: string; subtasks_count?: number | undefined; tags?: string[] | undefined; team?: string | null | undefined; thread?: string | null | undefined; updated_at?: string | undefined; user?: string | null | undefined }[] }[]; has_more: boolean }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.user !== undefined) {
      query["user"] = params?.user;
    }
    if (params?.org !== undefined) {
      query["org"] = params?.org;
    }
    if (params?.limit !== undefined) {
      query["limit"] = params?.limit;
    }
    if (params?.afterCursor !== undefined) {
      query["after_cursor"] = params?.afterCursor;
    }

    return this.http.request(`/api/v1/teams/${team}/tasks/blocker_cycles`, { query });
  }

  /**
   * Get task activity metrics for a team
   * Returns task activity metrics scoped to one team (the Network in ArchAgents).
   * `open` counts currently open or in-progress team tasks, `created` counts
   * tasks inserted in the UTC-day window, and `completed` counts tasks whose
   * `closed_at` falls in that window while still in a closed status. Hard-deleted
   * tasks are absent from the projection and therefore omitted. The daily series
   * is zero-filled across the requested window and includes reconstructed
   * end-of-day `open` stock (from remaining projection rows' inserted_at/closed_at).
   * Any authenticated team member may read this count-only Network summary.
   * Requests from callers without team access return 404 so team existence is
   * not disclosed.
   * @param team - Team ID (`tem_...`). Only tasks belonging to this team are returned.
   * @param params - Query parameters.
   * @param params.days - UTC-day window. One of 7, 30, 90, or 365; defaults to 30.
   * @returns Successful response
   */
  async metrics(team: string, params?: { days?: number }): Promise<{ completed: number; created: number; days: number; end_at: string; open: number; series: Record<string, unknown>[]; start_at: string }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.days !== undefined) {
      query["days"] = params?.days;
    }

    return this.http.request(`/api/v1/teams/${team}/tasks/metrics`, { query });
  }

  /**
   * List an owner's ready tasks
   * Returns open tasks with no unfinished blockers and no active session lease.
   * Readiness is calculated by the server from the current task projection. It is
   * a snapshot, not a reservation; claim a task lease before starting work.
   * Pass `explain=true` to include every open task with a stable readiness reason.
   * @param team - Team ID (`tem_...`). Only tasks belonging to this team are returned.
   * @param params - Query parameters.
   * @param params.user - User ID (`usr_...`) owning the tasks.
   * @param params.org - Optional organization context for privileged callers.
   * @param params.explain - Include blocked and actively leased open tasks with exclusion reasons.
   * @param params.assignedToMe - Only include tasks assigned to the authenticated user.
   * @param params.sourceScope - Only include tasks whose source matches this container. Must be supplied with `source_type` and `source_id`.
   * @param params.sourceType - Only include tasks whose source matches this object kind.
   * @param params.sourceId - Only include tasks whose source matches this object identity.
   * @param params.epic - Only include tasks with this exact epic label.
   * @param params.limit - Maximum number of readiness entries to return. Capped at 100.
   * @param params.afterCursor - Opaque cursor returned by the previous page.
   * @returns Successful response
   */
  async ready(team: string, params?: { user?: string; org?: string; explain?: boolean; assignedToMe?: boolean; sourceScope?: string; sourceType?: string; sourceId?: string; epic?: string; limit?: number; afterCursor?: string }): Promise<{ after_cursor?: string | undefined; authoritative: boolean; before_cursor?: string | undefined; data: { readiness: "ready" | "blocked" | "leased"; reason?: "open_blockers" | "active_lease" | null | undefined; task: { agent?: string | null | undefined; blocked_by_count?: number | undefined; closed_at?: string | null | undefined; comments_count?: number | undefined; created_at?: string | undefined; created_by_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; created_by_agent?: string | null | undefined; created_by_user?: string | null | undefined; current_lease?: { expires_at: string; harness: string; session_name: string } | null | undefined; description?: string | null | undefined; due_date?: string | null | undefined; epic?: string | null | undefined; id: string; is_blocked?: boolean | undefined; links?: Record<string, unknown> | undefined; metadata?: Record<string, unknown> | undefined; name: string; org?: string | null | undefined; owner_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; owner_agent?: string | null | undefined; owner_user?: string | null | undefined; parent?: string | null | undefined; priority?: number | undefined; sandbox?: string | null | undefined; source_id?: string | null | undefined; source_scope?: string | null | undefined; source_type?: string | null | undefined; status: string; subtasks_count?: number | undefined; tags?: string[] | undefined; team?: string | null | undefined; thread?: string | null | undefined; updated_at?: string | undefined; user?: string | null | undefined } }[]; has_more: boolean }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.user !== undefined) {
      query["user"] = params?.user;
    }
    if (params?.org !== undefined) {
      query["org"] = params?.org;
    }
    if (params?.explain !== undefined) {
      query["explain"] = params?.explain;
    }
    if (params?.assignedToMe !== undefined) {
      query["assigned_to_me"] = params?.assignedToMe;
    }
    if (params?.sourceScope !== undefined) {
      query["source_scope"] = params?.sourceScope;
    }
    if (params?.sourceType !== undefined) {
      query["source_type"] = params?.sourceType;
    }
    if (params?.sourceId !== undefined) {
      query["source_id"] = params?.sourceId;
    }
    if (params?.epic !== undefined) {
      query["epic"] = params?.epic;
    }
    if (params?.limit !== undefined) {
      query["limit"] = params?.limit;
    }
    if (params?.afterCursor !== undefined) {
      query["after_cursor"] = params?.afterCursor;
    }

    return this.http.request(`/api/v1/teams/${team}/tasks/ready`, { query });
  }

  /**
   * Search an owner's tasks
   * Performs a full-text search over tasks owned by the specified user or team and returns
   * matching results. Combine `q` with the optional filters to narrow the result set
   * further. When no query is provided, the endpoint behaves like a filtered list.
   * The `query` field in the response echoes the effective search query.
   * User-authenticated callers may search their personal tasks or tasks for teams
   * they have joined. Privileged callers provide the owner in the route; the owner's
   * organization is implied by that principal. An explicit `org` is optional and,
   * when set, must match the owner's organization.
   * @param team - Team ID (`tem_...`). Only tasks belonging to this team are returned.
   * @param params - Query parameters.
   * @param params.user - User ID (`usr_...`) whose tasks are searched.
   * @param params.org - Optional organization (`org_...`) for developer and server-to-server calls. When omitted, the org is taken from the owner principal (team, user, or agent). When set, it must match that principal's org; pass null for an owner outside an organization.
   * @param params.q - Full-text search query matched against task names and descriptions. Takes precedence over `query` when both are provided.
   * @param params.query - Alias for `q`. Use `q` when possible; this parameter exists for compatibility.
   * @param params.status - Filter results by status. One of `"open"`, `"in_progress"`, or `"done"`. Omit to include all statuses.
   * @param params.ownerUser - Restrict results to tasks assigned to the user with this public ID (`usr_...`).
   * @param params.ownerAgent - Restrict results to tasks assigned to the agent with this public ID (`agi_...`).
   * @param params.priority - Filter results by priority, from 0 (highest) to 4 (lowest).
   * @param params.tag - Return only tasks carrying this tag (matched against the canonical lowercase form).
   * @param params.parent - Return only subtasks of the given task (`tsk_...`), or pass `none` to return only top-level tasks.
   * @param params.sourceScope - Return only tasks whose source matches this container. Must be supplied with `source_type` and `source_id`.
   * @param params.sourceType - Return only tasks whose source matches this object kind.
   * @param params.sourceId - Return only tasks whose source matches this object identity.
   * @param params.epic - Return only tasks with this exact epic label.
   * @param params.limit - Maximum number of tasks to return. Capped at 100.
   * @param params.afterCursor - Opaque cursor returned by the previous page.
   * @returns Successful response
   */
  async search(team: string, params?: { user?: string; org?: string; q?: string; query?: string; status?: string; ownerUser?: string; ownerAgent?: string; priority?: number; tag?: string; parent?: string; sourceScope?: string; sourceType?: string; sourceId?: string; epic?: string; limit?: number; afterCursor?: string }): Promise<{ after_cursor?: string | undefined; before_cursor?: string | undefined; data: { agent?: string | null | undefined; blocked_by_count?: number | undefined; closed_at?: string | null | undefined; comments_count?: number | undefined; created_at?: string | undefined; created_by_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; created_by_agent?: string | null | undefined; created_by_user?: string | null | undefined; current_lease?: { expires_at: string; harness: string; session_name: string } | null | undefined; description?: string | null | undefined; due_date?: string | null | undefined; epic?: string | null | undefined; id: string; is_blocked?: boolean | undefined; links?: Record<string, unknown> | undefined; metadata?: Record<string, unknown> | undefined; name: string; org?: string | null | undefined; owner_actor?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined } | null | undefined; owner_agent?: string | null | undefined; owner_user?: string | null | undefined; parent?: string | null | undefined; priority?: number | undefined; sandbox?: string | null | undefined; source_id?: string | null | undefined; source_scope?: string | null | undefined; source_type?: string | null | undefined; status: string; subtasks_count?: number | undefined; tags?: string[] | undefined; team?: string | null | undefined; thread?: string | null | undefined; updated_at?: string | undefined; user?: string | null | undefined }[]; has_more: boolean; query: string }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.user !== undefined) {
      query["user"] = params?.user;
    }
    if (params?.org !== undefined) {
      query["org"] = params?.org;
    }
    if (params?.q !== undefined) {
      query["q"] = params?.q;
    }
    if (params?.query !== undefined) {
      query["query"] = params?.query;
    }
    if (params?.status !== undefined) {
      query["status"] = params?.status;
    }
    if (params?.ownerUser !== undefined) {
      query["owner_user"] = params?.ownerUser;
    }
    if (params?.ownerAgent !== undefined) {
      query["owner_agent"] = params?.ownerAgent;
    }
    if (params?.priority !== undefined) {
      query["priority"] = params?.priority;
    }
    if (params?.tag !== undefined) {
      query["tag"] = params?.tag;
    }
    if (params?.parent !== undefined) {
      query["parent"] = params?.parent;
    }
    if (params?.sourceScope !== undefined) {
      query["source_scope"] = params?.sourceScope;
    }
    if (params?.sourceType !== undefined) {
      query["source_type"] = params?.sourceType;
    }
    if (params?.sourceId !== undefined) {
      query["source_id"] = params?.sourceId;
    }
    if (params?.epic !== undefined) {
      query["epic"] = params?.epic;
    }
    if (params?.limit !== undefined) {
      query["limit"] = params?.limit;
    }
    if (params?.afterCursor !== undefined) {
      query["after_cursor"] = params?.afterCursor;
    }

    return this.http.request(`/api/v1/teams/${team}/tasks/search`, { query });
  }
}

export class TeamThreadResource {
  constructor(private http: HttpClient) {}

  /**
   * List threads for a team
   * Returns all threads owned by the specified team that the authenticated caller
   * has permission to view. The caller must have access to the team; requests
   * without team access are rejected with 404.
   * Threads are returned in a single `data` array, ordered with the team's
   * default thread first, then by most recent activity (newest first). Each
   * thread carries a `last_activity` timestamp — the most recent message's
   * creation time, falling back to the thread's own creation time. Use the
   * team-scoped thread endpoints to create, update, or delete individual
   * threads.
   * @param team - Team ID (`tem_...`) whose threads should be listed.
   * @param params - Query parameters.
   * @param params.tags - Optional: only return threads tagged with at least one of these tags (OR-match). Repeated query params, e.g. `?tags[]=blocked&tags[]=needs-review`.
   * @returns Successful response
   */
  async list(team: string, params?: { tags?: string[] }): Promise<{ data: { agent_user?: string | null | undefined; created_at?: string | undefined; creator?: string | { alias?: string | null | undefined; app?: string | null | undefined; app_name?: string | null | undefined; created_by_agent_user?: string | null | undefined; created_by_developer?: string | null | undefined; created_by_org?: string | null | undefined; created_by_team?: string | null | undefined; created_by_user?: string | null | undefined; email?: string | null | undefined; id: string; is_system_user?: boolean | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_name?: string | null | undefined; org_role?: string | null | undefined; org_slug?: string | null | undefined; sandbox?: string | null | undefined; sandbox_name?: string | null | undefined } | null | undefined; description?: string | null | undefined; id: string; is_channel?: boolean | undefined; is_default?: boolean | undefined; is_transient?: boolean | undefined; is_unlisted?: boolean | undefined; key?: string | null | undefined; kind?: string | null | undefined; last_activity?: string | null | undefined; last_message_preview?: string | null | undefined; last_message_sender?: string | null | undefined; metadata?: Record<string, unknown> | null | undefined; muted?: boolean | undefined; org?: string | null | undefined; parent_message?: { acl?: { add?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; grants?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; remove?: { principal?: string | undefined; principal_type: string }[] | undefined } | null | undefined; actors?: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined }[] | undefined; agent?: string | null | undefined; agent_mode?: "cli" | "embedded" | null | undefined; attachments?: { content_type?: string | null | undefined; description?: string | null | undefined; filename?: string | null | undefined; height?: number | null | undefined; id: string; image_height?: number | null | undefined; image_source?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined; image_url?: string | null | undefined; image_width?: number | null | undefined; media_type?: string | undefined; name?: string | null | undefined; object?: Record<string, unknown> | undefined; title?: string | null | undefined; type: string; url?: string | null | undefined; variants?: { content_type?: string | null | undefined; created_at?: string | undefined; file?: string | undefined; filename?: string | null | undefined; height?: number | null | undefined; id: string; image_source?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined; updated_at?: string | undefined; url?: string | null | undefined; variant_key?: string | undefined; width?: number | null | undefined }[] | undefined; version?: number | null | undefined; width?: number | null | undefined }[] | undefined; branched_thread?: string | null | undefined; content?: string | null | undefined; created_at?: string | undefined; has_replies?: boolean | undefined; id: string; idempotency_key?: string | null | undefined; is_deleted?: boolean | undefined; legacy_agent?: string | null | undefined; metadata?: Record<string, unknown> | undefined; org?: string | null | undefined; reactions?: { payload?: Record<string, unknown> | undefined; type: string; user?: string | undefined }[] | undefined; rendering_mode?: string | null | undefined; replies?: Record<string, unknown>[] | undefined; replies_after_cursor?: string | null | undefined; replies_before_cursor?: string | null | undefined; reply_count?: number | undefined; reply_to?: Record<string, unknown> | null | undefined; root_message_id?: string | null | undefined; sandbox?: string | null | undefined; team?: string | null | undefined; thread?: string | undefined; type?: string | null | undefined; user?: string | { alias?: string | null | undefined; app?: string | null | undefined; app_name?: string | null | undefined; created_by_agent_user?: string | null | undefined; created_by_developer?: string | null | undefined; created_by_org?: string | null | undefined; created_by_team?: string | null | undefined; created_by_user?: string | null | undefined; email?: string | null | undefined; id: string; is_system_user?: boolean | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_name?: string | null | undefined; org_role?: string | null | undefined; org_slug?: string | null | undefined; sandbox?: string | null | undefined; sandbox_name?: string | null | undefined } | null | undefined; visibility?: "default" | "private" | undefined } | null | undefined; participant?: string[] | undefined; participants?: { alias?: string | null | undefined; app?: string | null | undefined; app_name?: string | null | undefined; created_by_agent_user?: string | null | undefined; created_by_developer?: string | null | undefined; created_by_org?: string | null | undefined; created_by_team?: string | null | undefined; created_by_user?: string | null | undefined; email?: string | null | undefined; id: string; is_system_user?: boolean | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_name?: string | null | undefined; org_role?: string | null | undefined; org_slug?: string | null | undefined; sandbox?: string | null | undefined; sandbox_name?: string | null | undefined }[] | undefined; participating_actor?: string[] | undefined; participating_agents?: { acl?: { add?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; grants?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; remove?: { principal?: string | undefined; principal_type: string }[] | undefined } | null | undefined; app?: string | undefined; created_at?: string | undefined; default_model?: string | null | undefined; description?: string | null | undefined; email?: string | null | undefined; id: string; identity?: string | null | undefined; last_applied_template_config?: string | null | undefined; lookup_key?: string | null | undefined; metadata?: Record<string, unknown> | undefined; name?: string | undefined; org?: string | null | undefined; org_name?: string | null | undefined; originator?: string | null | undefined; phone_number?: string | null | undefined; sandbox?: string | null | undefined; source_solution?: { current_solution?: { category_keys?: string[] | undefined; created_at?: string | undefined; description?: string | null | undefined; events?: Record<string, unknown> | undefined; id: string; image_url?: string | null | undefined; kind: string; latest_solution?: string | null | undefined; latest_version?: string | null | undefined; lookup_key?: string | null | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_logo?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined; org_name?: string | null | undefined; org_slug?: string | null | undefined; owners: string[]; readme_url?: string | null | undefined; screenshot_urls?: string[] | undefined; solution_id?: string | null | undefined; solution_version?: string | null | undefined; tag_keys?: string[] | undefined; template_kind?: string | null | undefined; templates: { description?: string | null | undefined; details?: { automation_type?: string | null | undefined; invoke_contract?: { input_schema?: Record<string, unknown> | null | undefined; participants?: { description?: string | null | undefined; name: string; required: boolean; type: string }[] | null | undefined; prefills: { participants?: Record<string, unknown> | undefined; payload?: Record<string, unknown> | undefined } } | null | undefined; type: "automation" } | null | undefined; display_name?: string | null | undefined; id?: string | null | undefined; kind: string; lookup_key?: string | null | undefined; name?: string | null | undefined; readme_url?: string | null | undefined; virtual_path?: string | null | undefined }[]; updated_at?: string | undefined; upgrade_available: boolean; virtual_path?: string | null | undefined } | undefined; solution: { category_keys?: string[] | undefined; created_at?: string | undefined; description?: string | null | undefined; events?: Record<string, unknown> | undefined; id: string; image_url?: string | null | undefined; kind: string; latest_solution?: string | null | undefined; latest_version?: string | null | undefined; lookup_key?: string | null | undefined; metadata?: Record<string, unknown> | undefined; name?: string | null | undefined; org?: string | null | undefined; org_logo?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined; org_name?: string | null | undefined; org_slug?: string | null | undefined; owners: string[]; readme_url?: string | null | undefined; screenshot_urls?: string[] | undefined; solution_id?: string | null | undefined; solution_version?: string | null | undefined; tag_keys?: string[] | undefined; template_kind?: string | null | undefined; templates: { description?: string | null | undefined; details?: { automation_type?: string | null | undefined; invoke_contract?: { input_schema?: Record<string, unknown> | null | undefined; participants?: { description?: string | null | undefined; name: string; required: boolean; type: string }[] | null | undefined; prefills: { participants?: Record<string, unknown> | undefined; payload?: Record<string, unknown> | undefined } } | null | undefined; type: "automation" } | null | undefined; display_name?: string | null | undefined; id?: string | null | undefined; kind: string; lookup_key?: string | null | undefined; name?: string | null | undefined; readme_url?: string | null | undefined; virtual_path?: string | null | undefined }[]; updated_at?: string | undefined; upgrade_available: boolean; virtual_path?: string | null | undefined }; template: { created_at?: string | undefined; description?: string | null | undefined; display_name?: string | null | undefined; id: string; kind: string; lookup_key?: string | null | undefined; name?: string | null | undefined; updated_at?: string | undefined; virtual_path?: string | null | undefined } } | null | undefined; team?: string | null | undefined; template_upgrade_available?: boolean | null | undefined; updated_at?: string | undefined; user?: string | null | undefined }[] | null | undefined; role?: string | null | undefined; sandbox?: string | null | undefined; settings?: { agent_enabled?: boolean | null | undefined } | undefined; slug?: string | null | undefined; sub_threads?: Record<string, unknown>[] | null | undefined; tags?: string[] | undefined; team?: string | null | undefined; title?: string | null | undefined; ttl?: string | null | undefined; unread_count?: number | null | undefined; updated_at?: string | undefined; user?: string | null | undefined; visibility: "team" | "restricted" | "private" }[] }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.tags !== undefined) {
      query["tags"] = params?.tags;
    }

    return this.http.request(`/api/v1/teams/${team}/threads`, { query });
  }

  /**
   * Create a thread for a team
   * Creates a new thread owned by the specified team. The authenticated caller must
   * have access to the team; requests from callers without team access are rejected
   * with 404.
   * If a `profile_picture` is provided in the thread params, it must be
   * base64-encoded image data. The image is uploaded and associated with the thread
   * before creation completes. Omit `profile_picture` to skip this step.
   * By default the platform sends an automatic welcome message into the new thread.
   * Pass `skip_welcome_message: true` to suppress this behavior, for example when
   * creating threads programmatically in bulk or seeding test data.
   * @param team - Team ID (`tem_...`) whose threads should be listed.
   * @param input - Request body.
   * @param input.skip_welcome_message - When `true`, suppresses the automatic welcome message that is otherwise sent into the thread on creation. Defaults to `false`.
   * @param input.thread - Attributes for the new thread. See ThreadCreateParams for available fields.
   * @returns The newly created thread.
   */
  async create(team: string, input: { skip_welcome_message?: boolean | undefined; thread: { create_legacy_agent?: boolean | undefined; description?: string | undefined; is_unlisted?: boolean | undefined; key?: string | undefined; kind?: "personal" | undefined; members?: { id: string; type: "user" | "agent" }[] | undefined; metadata?: Record<string, unknown> | undefined; muted?: boolean | undefined; org_id?: string | undefined; profile_picture?: { data?: string | undefined; filename?: string | undefined; mime_type?: string | undefined } | undefined; settings?: { agent_enabled?: boolean | null | undefined } | undefined; slug?: string | undefined; title?: string | undefined; visibility?: "team" | "restricted" | "private" | undefined } }): Promise<Thread> {
    return this.http.request(`/api/v1/teams/${team}/threads`, { method: "POST", body: input });
  }

  /**
   * Get threads-created count for a team
   * Returns how many **network-public** team threads were created in the UTC-day
   * window. Counts come from realtime SQL aggregation over append-only
   * `network_activity_events` (`thread.created` rows co-committed with create):
   * team- or restricted-visibility threads (plus legacy open-team rows), never
   * private or mirror. Every Network member sees the same number. Hard-delete
   * co-commits a separate `thread.deleted` transition event and does not remove
   * create events (metric is "created", not "still present"). Visibility flips
   * after insert are ignored in v1.
   * Any authenticated team member may read this count-only Network summary.
   * Requests from callers without team access return 404 so team existence is
   * not disclosed.
   * @param team - Team ID (`tem_...`) whose threads should be listed.
   * @param params - Query parameters.
   * @param params.days - UTC-day window. One of 7, 30, 90, or 365; defaults to 30.
   * @returns Successful response
   */
  async metrics(team: string, params?: { days?: number }): Promise<{ days: number; end_at: string; opened: number; start_at: string }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.days !== undefined) {
      query["days"] = params?.days;
    }

    return this.http.request(`/api/v1/teams/${team}/threads/metrics`, { query });
  }
}

export class TeamResource {
  readonly custom_objects: TeamCustomObjectResource;
  readonly members: MemberResource;
  readonly tasks: TeamTaskResource;
  readonly threads: TeamThreadResource;

  constructor(private http: HttpClient) {
    this.custom_objects = new TeamCustomObjectResource(http);
    this.members = new MemberResource(http);
    this.tasks = new TeamTaskResource(http);
    this.threads = new TeamThreadResource(http);
  }

  /**
   * List teams
   * Returns a paginated list of teams visible to the authenticated user, ordered
   * by creation time descending. Use `membership` to narrow results to teams the
   * caller has joined or teams they are eligible to join based on their ACL
   * visibility.
   * Supports full-text search across team name and description via `search`, and
   * structured metadata filtering via `metadata`. When `app` is present, results
   * are scoped to that app and the caller must hold the corresponding app scope.
   * @param params - Query parameters.
   * @param params.page - Page number to retrieve, starting at 1. Defaults to 1.
   * @param params.pageSize - Number of teams to return per page. Defaults to 25.
   * @param params.search - Full-text search string matched against team name and description.
   * @param params.metadata - Structured metadata filter expression. Only teams whose metadata satisfies the expression are returned.
   * @param params.membership - Filter teams by membership status. `"joined"` returns only teams the caller is a member of. `"joinable"` returns ACL-visible teams the caller has not yet joined. Omit to return all visible teams.
   * @returns Successful response
   */
  async list(params?: { page?: number; pageSize?: number; search?: string; metadata?: { clause?: Record<string, unknown> | undefined; clauses?: Record<string, unknown>[] | undefined; key?: string | undefined; operator?: "and" | "or" | "eq" | "contains" | "exists" | "not" | undefined; path?: string[] | undefined; type?: string | undefined; value?: unknown | undefined }; membership?: string }): Promise<{ data: { acl?: { add?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; grants?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; remove?: { principal?: string | undefined; principal_type: string }[] | undefined } | null | undefined; app?: string | undefined; badges?: Record<string, unknown> | null | undefined; created_at?: string | undefined; description?: string | null | undefined; id: string; membership_status?: string | null | undefined; metadata?: Record<string, unknown> | undefined; name?: string | undefined; org?: string | null | undefined; sandbox?: string | null | undefined; slug?: string | null | undefined; updated_at?: string | undefined }[]; has_next: boolean; has_prev: boolean; page: number; page_size: number; total_entries: number; total_pages: number }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.page !== undefined) {
      query["page"] = params?.page;
    }
    if (params?.pageSize !== undefined) {
      query["page_size"] = params?.pageSize;
    }
    if (params?.search !== undefined) {
      query["search"] = params?.search;
    }
    if (params?.metadata !== undefined) {
      query["metadata"] = JSON.stringify(params?.metadata);
    }
    if (params?.membership !== undefined) {
      query["membership"] = params?.membership;
    }

    return this.http.request(`/api/v1/teams`, { query });
  }

  /**
   * Create a team
   * Creates a new team and returns the created team object. The authenticated
   * user becomes the team's owner.
   * When `app` is supplied, the request is scoped to that app and the caller
   * must hold the corresponding app scope. Omit `org` unless you want the team
   * pinned to a specific organization. A default chat thread is provisioned for
   * the team automatically after creation.
   * @param input - Request body.
   * @param input.acl - Access control configuration for the team. Controls who can discover and join the team.
   * @param input.description - Optional human-readable description of the team's purpose.
   * @param input.idempotency_key - Optional retry key. Replays in the same app, organization, and sandbox return the original team.
   * @param input.metadata - Arbitrary key-value pairs you can attach to the team for your own use. Values must be strings.
   * @param input.name - Display name for the team.
   * @param input.org - Organization ID (`org_...`) to associate the team with. Omit to create the team without an org affiliation.
   * @returns The newly created team.
   */
  async create(input: { acl?: { add?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; grants?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; remove?: { principal?: string | undefined; principal_type: string }[] | undefined } | undefined; description?: string | undefined; idempotency_key?: string | undefined; metadata?: Record<string, unknown> | undefined; name: string; org?: string | undefined }): Promise<Team> {
    return this.http.request(`/api/v1/teams`, { method: "POST", body: input });
  }

  /**
   * Join a team with an invite code
   * Adds a principal to a team using a 12-character invite code. The invite
   * code can be supplied as either `join_code` or `invite_code`; both are
   * accepted for backwards compatibility.
   * For user-authenticated requests, the currently authenticated user is added
   * to the team. For server-to-server requests, you must supply either `agent`
   * (to add an agent) or `user` (to add a specific user by ID). If the user
   * is already a member of the team, the request succeeds without creating a
   * duplicate membership.
   * This endpoint is rate-limited to 10 requests per minute per IP address to
   * prevent invite-code enumeration.
   * @param input - Request body.
   * @param input.agent - Agent ID (`agent_...`) to add to the team. When provided, the agent is joined instead of the authenticated user. Requires a server-to-server session.
   * @param input.invite_code - 12-character invite code — alias for `join_code` accepted for backwards compatibility.
   * @param input.join_code - 12-character invite code that identifies the team. Mutually usable with `invite_code`.
   * @param input.user - User ID (`user_...`) to add to the team. Required for server-to-server requests when `agent` is not supplied.
   * @returns The team the principal has joined.
   */
  async joinByCode(input: { agent?: string | undefined; invite_code?: string | undefined; join_code?: string | undefined; user?: string | undefined }): Promise<Team> {
    return this.http.request(`/api/v1/teams/join_by_code`, { method: "POST", body: input });
  }

  /**
   * Delete a team
   * Permanently deletes the team identified by `team`. This action is
   * irreversible — all team memberships, settings, and associated data are
   * removed.
   * The caller must be the team owner or an org admin. When `app` is present,
   * the caller must also hold the corresponding app scope.
   * @param team - Team ID (`team_...`) of the team to delete.
   * @returns Empty response — the team has been deleted.
   */
  async delete(team: string): Promise<void> {
    await this.http.request(`/api/v1/teams/${team}`, { method: "DELETE" });
  }

  /**
   * Retrieve a team
   * Returns the full team object for the given `team` ID, including its current
   * member list and all associated threads.
   * The authenticated user must be a member of the team or hold a role that
   * grants visibility (org admin, app scope). When `app` is supplied, the
   * caller must hold the corresponding app scope.
   * @param team - Team ID (`team_...`) of the team to retrieve.
   * @returns The requested team, including its members and threads.
   */
  async get(team: string): Promise<Team> {
    return this.http.request(`/api/v1/teams/${team}`);
  }

  /**
   * Update a team
   * Updates one or more attributes of the team identified by `team`. Only the
   * fields you provide are changed; omitted fields are left as-is.
   * To replace the team's profile picture, supply the `profile_picture` object
   * with base64-encoded image data. The previous picture is deleted after the
   * new one is successfully uploaded. When `app` is present, the caller must hold
   * the corresponding app scope. The caller must be a team owner or org admin.
   * @param team - Team ID (`team_...`) of the team to update.
   * @param input - Request body.
   * @param input.acl - New access control configuration for the team. Replaces the existing ACL.
   * @param input.description - New human-readable description of the team's purpose.
   * @param input.metadata - Arbitrary key-value pairs to set on the team. Replaces the existing metadata map entirely.
   * @param input.name - New display name for the team.
   * @param input.profile_picture - New profile picture for the team. Provide this object to upload and replace the current picture.
   * @returns The updated team with all changes applied.
   */
  async update(team: string, input: { acl?: { add?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; grants?: { actions: string[]; principal?: string | undefined; principal_type: string }[] | undefined; remove?: { principal?: string | undefined; principal_type: string }[] | undefined } | undefined; description?: string | undefined; metadata?: Record<string, unknown> | undefined; name?: string | undefined; profile_picture?: { data?: string | undefined; filename?: string | undefined; mime_type?: string | undefined } | undefined }): Promise<Team> {
    return this.http.request(`/api/v1/teams/${team}`, { method: "PATCH", body: input });
  }

  /**
   * List a team's artifacts
   * Returns all artifacts owned by the specified team. Artifacts represent
   * AI-generated or user-uploaded files associated with agent sessions,
   * threads, or sandboxes — such as images, documents, and code outputs.
   * The authenticated user must be a member of the team. Attempting to list
   * artifacts for a team the caller does not have access to returns 404
   * rather than 403 to avoid leaking team existence.
   * Results are returned in a single page without cursor pagination. Each
   * artifact in the response reflects the state of its current version,
   * including a short-lived signed `file_url` for direct download.
   * @param team - Team ID (`tea_...`). The authenticated user must be a member of this team.
   * @returns Successful response
   */
  async artifacts(team: string): Promise<{ data: { agent?: string | undefined; content_type?: string | undefined; created_at?: string | undefined; current_version?: string | undefined; description?: string | undefined; file?: string | undefined; file_name?: string | undefined; file_url?: string | undefined; id: string; image_source?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | undefined; name?: string | undefined; org?: string | undefined; sandbox?: string | undefined; team?: string | undefined; thread?: string | undefined; updated_at?: string | undefined; user?: string | undefined; version?: number | undefined }[] }> {
    return this.http.request(`/api/v1/teams/${team}/artifacts`);
  }

  /**
   * Create a team invite
   * Generates a new invite code for the specified team. The authenticated user
   * must be a member of the team with the `owner` or `admin` role.
   * The returned code is a short alphanumeric string that other users can
   * present to join the team. Each call produces a new code; previously issued
   * codes are not invalidated by this request.
   * @param team - Team ID (`tm_...`) identifying the team for which to generate the invite code.
   * @returns The newly created team invite containing the join code.
   */
  async invite(team: string): Promise<TeamInvite> {
    return this.http.request(`/api/v1/teams/${team}/invite`, { method: "POST" });
  }

  /**
   * Create a team invite (server-to-server)
   * Generates a new invite code for the specified team using server-to-server
   * authentication. Unlike the user-facing create endpoint, this variant does not
   * require the caller to be a team member — it is intended for privileged
   * back-end services acting on behalf of your platform.
   * The returned code is a short alphanumeric string that users can present to
   * join the team. Each call produces a new code; previously issued codes are
   * not invalidated by this request.
   * @param team - Team ID (`tm_...`) identifying the team for which to generate the invite code.
   * @returns Successful response
   */
  async invites(team: string): Promise<{ code: string }> {
    return this.http.request(`/api/v1/teams/${team}/invites`, { method: "POST" });
  }

  /**
   * Join a team
   * Adds a principal to a team that is visible to the authenticated user.
   * By default, the currently authenticated user joins the team. Provide `agent`
   * to add an agent to the team instead — the caller must already be a member of
   * the team to do so. Provide `user` (by ID) or `email` to add another user from
   * your organization — the caller must be a team owner, team admin, or org admin.
   * Only one of `agent`, `user`, or `email` may be supplied per request.
   * If the target principal is already a member of the team, the request succeeds
   * without creating a duplicate membership. Server-to-server callers are not
   * permitted to use this endpoint; use the invite-code endpoint instead.
   * @param team - Team ID (`team_...`) to join.
   * @param input - Request body.
   * @param input.agent - Agent ID (`agent_...`) to add to the team. The caller must already be a member of the team.
   * @param input.email - Email address of a member of the caller's organization to add to the team. Requires team-owner, team-admin, or org-admin role.
   * @param input.user - User ID (`user_...`) of a member of the caller's organization to add to the team. Requires team-owner, team-admin, or org-admin role.
   * @returns Empty response — the principal is now a member of the team.
   */
  async join(team: string, input: { agent?: string | undefined; email?: string | undefined; user?: string | undefined }): Promise<void> {
    await this.http.request(`/api/v1/teams/${team}/join`, { method: "POST", body: input });
  }

  /**
   * Leave a team
   * Removes a principal from a team. By default, the authenticated user removes
   * themselves from the team. Provide `agent` to remove an agent instead — the
   * caller must be a member of the team to do so.
   * Team owners cannot leave their own team. To transfer ownership first, use
   * the update-membership endpoint, then call this endpoint.
   * For server-to-server requests, `user` is required to identify which user
   * should be removed.
   * @param team - Team ID (`team_...`) to leave.
   * @returns Empty response — the principal has been removed from the team.
   */
  async leave(team: string): Promise<void> {
    await this.http.request(`/api/v1/teams/${team}/leave`, { method: "DELETE" });
  }

  /**
   * List task assignees for a team
   * Returns the list of principals — users and agents — that can be assigned
   * to tasks within the team. Results include both human members and AI agents
   * and are sorted alphabetically by display name.
   * This endpoint is only available for team-scoped contexts. Calling it with a
   * user-scoped owner returns a 400 error. The authenticated user must be a
   * member of the team or hold org-admin access. App-scoped developer and
   * server-to-server callers may list assignees for teams in their app so they
   * can select the explicit user or agent actor required by privileged task
   * mutations.
   * @param team - Team ID (`tem_...`).
   * @param params - Query parameters.
   * @param params.org - Explicit organization (`org_...`) for developer and server-to-server calls. Pass null for a team outside an organization.
   * @returns Successful response
   */
  async taskAssignees(team: string, params?: { org?: string }): Promise<{ data: { actor: { alias?: string | null | undefined; id?: string | null | undefined; name?: string | null | undefined; profile_picture?: { file?: string | null | undefined; height?: number | null | undefined; media?: string | null | undefined; mime_type?: string | null | undefined; refresh_url?: string | null | undefined; url?: string | null | undefined; width?: number | null | undefined } | null | undefined }; id: string; type: "user" | "agent" }[] }> {
    const query: Record<string, string | number | boolean | Array<string | number | boolean> | undefined> = {};
    if (params?.org !== undefined) {
      query["org"] = params?.org;
    }

    return this.http.request(`/api/v1/teams/${team}/task_assignees`, { query });
  }
}
