// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 42e415150ab8

import type { Channel } from "../phx_channel/channel.js";
import type { Socket } from "../phx_channel/socket.js";

/**
 * Channel for real-time chat messaging.
 * Supports team-scoped and user-scoped threads with keyed, transient, and direct
 * thread access patterns.
 */
export class ApiChatChannel {
  constructor(private channel: Channel, public readonly joinResponse?: unknown) {}

  /** Join a team-scoped thread by ID */
  static topicTeamThread(teamId: string, threadId: string): string {
    return `api:chat:team:${teamId}:thread:${threadId}`;
  }

  /** Join a team-scoped thread by ID */
  static async joinTeamThread(socket: Socket, teamId: string, threadId: string, payload?: { after_cursor?: string; before_cursor?: string; include_metadata?: boolean; limit?: number }): Promise<ApiChatChannel> {
    const topic = ApiChatChannel.topicTeamThread(teamId, threadId);
    const channel = socket.channel(topic);
    const joinResponse = await channel.join(payload);
    return new ApiChatChannel(channel, joinResponse);
  }

  /** Join or create a team-scoped keyed thread */
  static topicTeamKeyed(teamId: string, key: string): string {
    return `api:chat:team:${teamId}:key:${key}`;
  }

  /** Join or create a team-scoped keyed thread */
  static async joinTeamKeyed(socket: Socket, teamId: string, key: string, payload?: { after_cursor?: string; before_cursor?: string; include_metadata?: boolean; limit?: number }): Promise<ApiChatChannel> {
    const topic = ApiChatChannel.topicTeamKeyed(teamId, key);
    const channel = socket.channel(topic);
    const joinResponse = await channel.join(payload);
    return new ApiChatChannel(channel, joinResponse);
  }

  /** Join a team-scoped transient (ephemeral) thread */
  static topicTeamTransient(teamId: string, key: string): string {
    return `api:chat:team:${teamId}:transient:${key}`;
  }

  /** Join a team-scoped transient (ephemeral) thread */
  static async joinTeamTransient(socket: Socket, teamId: string, key: string, payload?: { after_cursor?: string; before_cursor?: string; include_metadata?: boolean; limit?: number }): Promise<ApiChatChannel> {
    const topic = ApiChatChannel.topicTeamTransient(teamId, key);
    const channel = socket.channel(topic);
    const joinResponse = await channel.join(payload);
    return new ApiChatChannel(channel, joinResponse);
  }

  /** Join a user-scoped thread by ID */
  static topicUserThread(threadId: string): string {
    return `api:chat:user:thread:${threadId}`;
  }

  /** Join a user-scoped thread by ID */
  static async joinUserThread(socket: Socket, threadId: string, payload?: { after_cursor?: string; before_cursor?: string; include_metadata?: boolean; limit?: number }): Promise<ApiChatChannel> {
    const topic = ApiChatChannel.topicUserThread(threadId);
    const channel = socket.channel(topic);
    const joinResponse = await channel.join(payload);
    return new ApiChatChannel(channel, joinResponse);
  }

  /** Join or create a user-scoped keyed thread */
  static topicUserKeyed(key: string): string {
    return `api:chat:user:key:${key}`;
  }

  /** Join or create a user-scoped keyed thread */
  static async joinUserKeyed(socket: Socket, key: string, payload?: { after_cursor?: string; before_cursor?: string; include_metadata?: boolean; limit?: number }): Promise<ApiChatChannel> {
    const topic = ApiChatChannel.topicUserKeyed(key);
    const channel = socket.channel(topic);
    const joinResponse = await channel.join(payload);
    return new ApiChatChannel(channel, joinResponse);
  }

  /** Join a user-scoped transient (ephemeral) thread */
  static topicUserTransient(key: string): string {
    return `api:chat:user:transient:${key}`;
  }

  /** Join a user-scoped transient (ephemeral) thread */
  static async joinUserTransient(socket: Socket, key: string, payload?: { after_cursor?: string; before_cursor?: string; include_metadata?: boolean; limit?: number }): Promise<ApiChatChannel> {
    const topic = ApiChatChannel.topicUserTransient(key);
    const channel = socket.channel(topic);
    const joinResponse = await channel.join(payload);
    return new ApiChatChannel(channel, joinResponse);
  }

  /** Leave the underlying channel. */
  async leave(): Promise<void> {
    await this.channel.leave();
  }

  /** Fork a sub-thread from an existing message */
  async apiChatForkThread(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:fork_thread", payload);
  }

  /** Mark a thread as read up to a given message */
  async apiChatMarkThreadRead(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:mark_thread_read", payload);
  }

  /** List all messages in the current thread */
  async apiChatListMessages(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:list_messages", payload);
  }

  /** Load additional messages with cursor-based pagination */
  async apiChatLoadMoreMessages(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:load_more_messages", payload);
  }

  /** Post a new message with optional uploads and reply-to */
  async apiChatPostMessage(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:post_message", payload);
  }

  /** Post a simple text message */
  async apiChatPostSimpleMessage(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:post_simple_message", payload);
  }

  /** Edit an existing message's content */
  async apiChatEditMessage(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:edit_message", payload);
  }

  /** Delete a message */
  async apiChatDeleteMessage(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:delete_message", payload);
  }

  /** Add an emoji reaction to a message */
  async apiChatAddReaction(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:add_reaction", payload);
  }

  /** Remove an emoji reaction from a message */
  async apiChatRemoveReaction(payload: Record<string, unknown>): Promise<unknown> {
    return this.channel.push("api:chat:remove_reaction", payload);
  }

  /** Broadcast when a new message is added to a thread */
  onMessageAdded(callback: (payload: unknown) => void): () => void {
    return this.channel.on("message_added", callback);
  }

  /** Broadcast when a message is updated or removed */
  onMessageUpdated(callback: (payload: unknown) => void): () => void {
    return this.channel.on("message_updated", callback);
  }

  /** Broadcast thread-level events (agent updates, read state, unread counts) */
  onThreadEvent(callback: (payload: unknown) => void): () => void {
    return this.channel.on("thread_event", callback);
  }

  /** Broadcast system-wide events */
  onSystemEvent(callback: (payload: unknown) => void): () => void {
    return this.channel.on("system_event", callback);
  }
}
