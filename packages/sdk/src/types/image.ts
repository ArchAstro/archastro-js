// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: a82bf4fa7e42

import { z } from "zod";

/** Resolved metadata for an image, including its delivery URL, dimensions, and optional references to the underlying storage file or media record. */
export interface ImageSource {
  /** ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file. */
  file?: string | null | undefined;
  /** Height of the image in pixels. `null` if not known. */
  height?: number | null | undefined;
  /** ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity. */
  media?: string | null | undefined;
  /** MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known. */
  mime_type?: string | null | undefined;
  /** Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing. */
  refresh_url?: string | null | undefined;
  /** Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires. */
  url?: string | null | undefined;
  /** Width of the image in pixels. `null` if not known. */
  width?: number | null | undefined;
}

/** Runtime validator for {@link ImageSource}. */
export const imageSourceSchema = z.object({
  /** ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file. */
  file: z.string().nullable().optional().describe("ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."),
  /** Height of the image in pixels. `null` if not known. */
  height: z.number().int().nullable().optional().describe("Height of the image in pixels. `null` if not known."),
  /** ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity. */
  media: z.string().nullable().optional().describe("ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."),
  /** MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known. */
  mime_type: z.string().nullable().optional().describe("MIME type of the image, e.g. `\"image/png\"` or `\"image/jpeg\"`. `null` if not known."),
  /** Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing. */
  refresh_url: z.string().nullable().optional().describe("Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."),
  /** Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires. */
  url: z.string().nullable().optional().describe("Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."),
  /** Width of the image in pixels. `null` if not known. */
  width: z.number().int().nullable().optional().describe("Width of the image in pixels. `null` if not known."),
}).describe("Resolved metadata for an image, including its delivery URL, dimensions, and optional references to the underlying storage file or media record.") satisfies z.ZodType<ImageSource>;
