// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: f3dbff31b43b

import { z } from "zod";
import { actorSchema } from "./common.js";
import type { Actor } from "./common.js";

/** Viewer-safe details about a task's current coding-session lease. */
export interface TaskSessionLeaseSummary {
  /** Server-calculated lease expiry in ISO 8601 format. */
  expires_at: string;
  /** Bounded harness identifier for the coding session. */
  harness: string;
  /** Display name supplied by the coding session that holds the lease. */
  session_name: string;
}

/** Runtime validator for {@link TaskSessionLeaseSummary}. */
export const taskSessionLeaseSummarySchema = z.object({
  /** Server-calculated lease expiry in ISO 8601 format. */
  expires_at: z.string().describe("Server-calculated lease expiry in ISO 8601 format."),
  /** Bounded harness identifier for the coding session. */
  harness: z.string().describe("Bounded harness identifier for the coding session."),
  /** Display name supplied by the coding session that holds the lease. */
  session_name: z.string().describe("Display name supplied by the coding session that holds the lease."),
}).describe("Viewer-safe details about a task's current coding-session lease.") satisfies z.ZodType<TaskSessionLeaseSummary>;

/** A task representing a unit of work, optionally assignable to a user or agent. */
export interface Task {
  /** ID of the agent that owns this task (`agi_...`). `null` if the task is scoped to a team or user. */
  agent?: string | null | undefined;
  /** Event-stream version for optimistic update fencing. Pass this value as `expected_version` on an update to reject concurrent writes. */
  aggregate_version?: number | null | undefined;
  /** Number of tasks marked as blocking this task, whether or not they are done (see `GET /tasks/{task}/blockers`). Computed on list/show reads; create/update responses may lag one read behind. */
  blocked_by_count?: number | undefined;
  /** When the task was marked as done or otherwise closed (ISO 8601). `null` if the task is still open. */
  closed_at?: string | null | undefined;
  /** Total number of comments posted on this task. */
  comments_count?: number | undefined;
  /** When the task was created (ISO 8601). */
  created_at?: string | undefined;
  /** Resolved creator details including `id`, `name`, `alias`, and `profile_picture`. `null` if no creator is set or the creator cannot be resolved (e.g. creating agent was deleted). */
  created_by_actor?: Actor | null | undefined;
  /** ID of the agent that created this task (`agi_...`). `null` if the task was created by a human user, or if the creating agent was later deleted. */
  created_by_agent?: string | null | undefined;
  /** ID of the user who created this task (`usr_...`). `null` if the task was created by an agent, or if creator provenance was cleared after the creator was deleted. */
  created_by_user?: string | null | undefined;
  /** Viewer-safe live coding-session lease summary. `null` when the task is unleased or the projected lease has expired. Fencing identifiers are never included. */
  current_lease?: TaskSessionLeaseSummary | null | undefined;
  /** Long-form description or notes for the task. `null` if no description has been provided. */
  description?: string | null | undefined;
  /** Date and time by which the task should be completed (ISO 8601). `null` if no due date is set. */
  due_date?: string | null | undefined;
  /** Free-form grouping label. `null` when the task is not in an epic. */
  epic?: string | null | undefined;
  /** Task ID (`tsk_...`). */
  id: string;
  /** `true` while at least one blocking task is not yet done. Informational only — a blocked task can still change status — and derived at read time, so the task un-blocks automatically when its last open blocker completes. Computed on list/show reads; create/update responses report `false` until the next read. */
  is_blocked?: boolean | undefined;
  /** Key-value map of named URLs or references associated with the task. Returns an empty object when no links have been set. */
  links?: Record<string, unknown> | undefined;
  /** Arbitrary key-value map of application-specific data stored alongside the task. Returns an empty object when no metadata has been set. */
  metadata?: Record<string, unknown> | undefined;
  /** Human-readable title of the task. */
  name: string;
  /** ID of the organization this task belongs to (`org_...`). `null` for tasks outside an org context. */
  org?: string | null | undefined;
  /** Resolved owner details including `id`, `name`, `alias`, and `profile_picture`. `null` if the task is unassigned or the owner cannot be resolved (e.g. assigned agent was deleted). */
  owner_actor?: Actor | null | undefined;
  /** ID of the agent assigned as owner (`agi_...`). `null` if the owner is a human user, the task is unassigned, or the assigned agent was deleted. */
  owner_agent?: string | null | undefined;
  /** ID of the user assigned as owner (`usr_...`). `null` if the owner is an agent, the task is unassigned, or the assigned agent was deleted. */
  owner_user?: string | null | undefined;
  /** ID of the parent task when this task is a subtask (`tsk_...`). `null` for top-level tasks. Subtasks nest exactly one level. */
  parent?: string | null | undefined;
  /** Priority level of the task from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when not explicitly set. */
  priority?: number | undefined;
  /** ID of the developer sandbox this task is scoped to (`dsb_...`). `null` for tasks outside a sandbox environment. */
  sandbox?: string | null | undefined;
  /** Source object identity (for example `ArchAstro/firstlanding`). `null` when the task has no source. */
  source_id?: string | null | undefined;
  /** Container of the work this task is about (for example `github.com`). `null` when the task has no source. Set together with `source_type` and `source_id`. */
  source_scope?: string | null | undefined;
  /** Kind of source object (for example `repository`). `null` when the task has no source. */
  source_type?: string | null | undefined;
  /** Current status of the task. One of `"open"`, `"in_progress"`, or `"done"`. */
  status: string;
  /** Number of subtasks under this task. Computed on list/show reads; create/update responses may report 0 until the next read. Always 0 for subtasks. */
  subtasks_count?: number | undefined;
  /** Labels for grouping and filtering, stored lowercase and de-duplicated. Empty array when untagged. */
  tags?: string[] | undefined;
  /** ID of the team that owns this task (`tem_...`). `null` if the task is not scoped to a team. */
  team?: string | null | undefined;
  /** ID of the thread this task is bound to (`thr_...`) — the conversation it was filed from, or the thread passed at creation. `null` for tasks not tied to a thread. */
  thread?: string | null | undefined;
  /** When the task was last modified (ISO 8601). */
  updated_at?: string | undefined;
  /** ID of the user that owns this task (`usr_...`). `null` if the task is scoped to a team. */
  user?: string | null | undefined;
}

/** Runtime validator for {@link Task}. */
export const taskSchema = z.object({
  /** ID of the agent that owns this task (`agi_...`). `null` if the task is scoped to a team or user. */
  agent: z.string().nullable().optional().describe("ID of the agent that owns this task (`agi_...`). `null` if the task is scoped to a team or user."),
  /** Event-stream version for optimistic update fencing. Pass this value as `expected_version` on an update to reject concurrent writes. */
  aggregate_version: z.number().int().nullable().optional().describe("Event-stream version for optimistic update fencing. Pass this value as `expected_version` on an update to reject concurrent writes."),
  /** Number of tasks marked as blocking this task, whether or not they are done (see `GET /tasks/{task}/blockers`). Computed on list/show reads; create/update responses may lag one read behind. */
  blocked_by_count: z.number().int().optional().describe("Number of tasks marked as blocking this task, whether or not they are done (see `GET /tasks/{task}/blockers`). Computed on list/show reads; create/update responses may lag one read behind."),
  /** When the task was marked as done or otherwise closed (ISO 8601). `null` if the task is still open. */
  closed_at: z.string().nullable().optional().describe("When the task was marked as done or otherwise closed (ISO 8601). `null` if the task is still open."),
  /** Total number of comments posted on this task. */
  comments_count: z.number().int().optional().describe("Total number of comments posted on this task."),
  /** When the task was created (ISO 8601). */
  created_at: z.string().optional().describe("When the task was created (ISO 8601)."),
  /** Resolved creator details including `id`, `name`, `alias`, and `profile_picture`. `null` if no creator is set or the creator cannot be resolved (e.g. creating agent was deleted). */
  created_by_actor: actorSchema.nullable().optional().describe("Resolved creator details including `id`, `name`, `alias`, and `profile_picture`. `null` if no creator is set or the creator cannot be resolved (e.g. creating agent was deleted)."),
  /** ID of the agent that created this task (`agi_...`). `null` if the task was created by a human user, or if the creating agent was later deleted. */
  created_by_agent: z.string().nullable().optional().describe("ID of the agent that created this task (`agi_...`). `null` if the task was created by a human user, or if the creating agent was later deleted."),
  /** ID of the user who created this task (`usr_...`). `null` if the task was created by an agent, or if creator provenance was cleared after the creator was deleted. */
  created_by_user: z.string().nullable().optional().describe("ID of the user who created this task (`usr_...`). `null` if the task was created by an agent, or if creator provenance was cleared after the creator was deleted."),
  /** Viewer-safe live coding-session lease summary. `null` when the task is unleased or the projected lease has expired. Fencing identifiers are never included. */
  current_lease: taskSessionLeaseSummarySchema.nullable().optional().describe("Viewer-safe live coding-session lease summary. `null` when the task is unleased or the projected lease has expired. Fencing identifiers are never included."),
  /** Long-form description or notes for the task. `null` if no description has been provided. */
  description: z.string().nullable().optional().describe("Long-form description or notes for the task. `null` if no description has been provided."),
  /** Date and time by which the task should be completed (ISO 8601). `null` if no due date is set. */
  due_date: z.string().nullable().optional().describe("Date and time by which the task should be completed (ISO 8601). `null` if no due date is set."),
  /** Free-form grouping label. `null` when the task is not in an epic. */
  epic: z.string().nullable().optional().describe("Free-form grouping label. `null` when the task is not in an epic."),
  /** Task ID (`tsk_...`). */
  id: z.string().describe("Task ID (`tsk_...`)."),
  /** `true` while at least one blocking task is not yet done. Informational only — a blocked task can still change status — and derived at read time, so the task un-blocks automatically when its last open blocker completes. Computed on list/show reads; create/update responses report `false` until the next read. */
  is_blocked: z.boolean().optional().describe("`true` while at least one blocking task is not yet done. Informational only — a blocked task can still change status — and derived at read time, so the task un-blocks automatically when its last open blocker completes. Computed on list/show reads; create/update responses report `false` until the next read."),
  /** Key-value map of named URLs or references associated with the task. Returns an empty object when no links have been set. */
  links: z.record(z.unknown()).optional().describe("Key-value map of named URLs or references associated with the task. Returns an empty object when no links have been set."),
  /** Arbitrary key-value map of application-specific data stored alongside the task. Returns an empty object when no metadata has been set. */
  metadata: z.record(z.unknown()).optional().describe("Arbitrary key-value map of application-specific data stored alongside the task. Returns an empty object when no metadata has been set."),
  /** Human-readable title of the task. */
  name: z.string().describe("Human-readable title of the task."),
  /** ID of the organization this task belongs to (`org_...`). `null` for tasks outside an org context. */
  org: z.string().nullable().optional().describe("ID of the organization this task belongs to (`org_...`). `null` for tasks outside an org context."),
  /** Resolved owner details including `id`, `name`, `alias`, and `profile_picture`. `null` if the task is unassigned or the owner cannot be resolved (e.g. assigned agent was deleted). */
  owner_actor: actorSchema.nullable().optional().describe("Resolved owner details including `id`, `name`, `alias`, and `profile_picture`. `null` if the task is unassigned or the owner cannot be resolved (e.g. assigned agent was deleted)."),
  /** ID of the agent assigned as owner (`agi_...`). `null` if the owner is a human user, the task is unassigned, or the assigned agent was deleted. */
  owner_agent: z.string().nullable().optional().describe("ID of the agent assigned as owner (`agi_...`). `null` if the owner is a human user, the task is unassigned, or the assigned agent was deleted."),
  /** ID of the user assigned as owner (`usr_...`). `null` if the owner is an agent, the task is unassigned, or the assigned agent was deleted. */
  owner_user: z.string().nullable().optional().describe("ID of the user assigned as owner (`usr_...`). `null` if the owner is an agent, the task is unassigned, or the assigned agent was deleted."),
  /** ID of the parent task when this task is a subtask (`tsk_...`). `null` for top-level tasks. Subtasks nest exactly one level. */
  parent: z.string().nullable().optional().describe("ID of the parent task when this task is a subtask (`tsk_...`). `null` for top-level tasks. Subtasks nest exactly one level."),
  /** Priority level of the task from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when not explicitly set. */
  priority: z.number().int().optional().describe("Priority level of the task from `0` (highest) to `4` (lowest). Defaults to `2` (medium) when not explicitly set."),
  /** ID of the developer sandbox this task is scoped to (`dsb_...`). `null` for tasks outside a sandbox environment. */
  sandbox: z.string().nullable().optional().describe("ID of the developer sandbox this task is scoped to (`dsb_...`). `null` for tasks outside a sandbox environment."),
  /** Source object identity (for example `ArchAstro/firstlanding`). `null` when the task has no source. */
  source_id: z.string().nullable().optional().describe("Source object identity (for example `ArchAstro/firstlanding`). `null` when the task has no source."),
  /** Container of the work this task is about (for example `github.com`). `null` when the task has no source. Set together with `source_type` and `source_id`. */
  source_scope: z.string().nullable().optional().describe("Container of the work this task is about (for example `github.com`). `null` when the task has no source. Set together with `source_type` and `source_id`."),
  /** Kind of source object (for example `repository`). `null` when the task has no source. */
  source_type: z.string().nullable().optional().describe("Kind of source object (for example `repository`). `null` when the task has no source."),
  /** Current status of the task. One of `"open"`, `"in_progress"`, or `"done"`. */
  status: z.string().describe("Current status of the task. One of `\"open\"`, `\"in_progress\"`, or `\"done\"`."),
  /** Number of subtasks under this task. Computed on list/show reads; create/update responses may report 0 until the next read. Always 0 for subtasks. */
  subtasks_count: z.number().int().optional().describe("Number of subtasks under this task. Computed on list/show reads; create/update responses may report 0 until the next read. Always 0 for subtasks."),
  /** Labels for grouping and filtering, stored lowercase and de-duplicated. Empty array when untagged. */
  tags: z.array(z.string()).optional().describe("Labels for grouping and filtering, stored lowercase and de-duplicated. Empty array when untagged."),
  /** ID of the team that owns this task (`tem_...`). `null` if the task is not scoped to a team. */
  team: z.string().nullable().optional().describe("ID of the team that owns this task (`tem_...`). `null` if the task is not scoped to a team."),
  /** ID of the thread this task is bound to (`thr_...`) — the conversation it was filed from, or the thread passed at creation. `null` for tasks not tied to a thread. */
  thread: z.string().nullable().optional().describe("ID of the thread this task is bound to (`thr_...`) — the conversation it was filed from, or the thread passed at creation. `null` for tasks not tied to a thread."),
  /** When the task was last modified (ISO 8601). */
  updated_at: z.string().optional().describe("When the task was last modified (ISO 8601)."),
  /** ID of the user that owns this task (`usr_...`). `null` if the task is scoped to a team. */
  user: z.string().nullable().optional().describe("ID of the user that owns this task (`usr_...`). `null` if the task is scoped to a team."),
}).describe("A task representing a unit of work, optionally assignable to a user or agent.") satisfies z.ZodType<Task>;

/** A comment posted on a task by a user or an agent, including resolved author information. */
export interface TaskComment {
  /** Resolved author details including `id`, `name`, `alias`, and `profile_picture`. `null` if no author is set or the author cannot be resolved (e.g. authoring agent was deleted). */
  author_actor?: Actor | undefined;
  /** ID of the agent that posted this comment (`agi_...`). `null` if the author is a human user, or if the authoring agent was later deleted. */
  author_agent?: string | undefined;
  /** ID of the user who posted this comment (`usr_...`). `null` if the author is an agent, or if author provenance was cleared after the authoring agent was deleted. */
  author_user?: string | undefined;
  /** Plain-text body of the comment. */
  body: string;
  /** When this comment was posted (ISO 8601). */
  created_at?: string | undefined;
  /** Comment ID (`tcmt_...`). */
  id: string;
  /** ID of the organization that owns this comment (`org_...`). */
  org?: string | undefined;
  /** Sandbox ID this comment is scoped to. `null` for comments outside a sandbox environment. */
  sandbox?: string | undefined;
  /** ID of the task this comment belongs to (`tsk_...`). */
  task?: string | undefined;
  /** ID of the team the task belongs to (`tem_...`). `null` if not scoped to a team. */
  team?: string | undefined;
  /** When this comment was last edited (ISO 8601). */
  updated_at?: string | undefined;
}

/** Runtime validator for {@link TaskComment}. */
export const taskCommentSchema = z.object({
  /** Resolved author details including `id`, `name`, `alias`, and `profile_picture`. `null` if no author is set or the author cannot be resolved (e.g. authoring agent was deleted). */
  author_actor: actorSchema.optional().describe("Resolved author details including `id`, `name`, `alias`, and `profile_picture`. `null` if no author is set or the author cannot be resolved (e.g. authoring agent was deleted)."),
  /** ID of the agent that posted this comment (`agi_...`). `null` if the author is a human user, or if the authoring agent was later deleted. */
  author_agent: z.string().optional().describe("ID of the agent that posted this comment (`agi_...`). `null` if the author is a human user, or if the authoring agent was later deleted."),
  /** ID of the user who posted this comment (`usr_...`). `null` if the author is an agent, or if author provenance was cleared after the authoring agent was deleted. */
  author_user: z.string().optional().describe("ID of the user who posted this comment (`usr_...`). `null` if the author is an agent, or if author provenance was cleared after the authoring agent was deleted."),
  /** Plain-text body of the comment. */
  body: z.string().describe("Plain-text body of the comment."),
  /** When this comment was posted (ISO 8601). */
  created_at: z.string().optional().describe("When this comment was posted (ISO 8601)."),
  /** Comment ID (`tcmt_...`). */
  id: z.string().describe("Comment ID (`tcmt_...`)."),
  /** ID of the organization that owns this comment (`org_...`). */
  org: z.string().optional().describe("ID of the organization that owns this comment (`org_...`)."),
  /** Sandbox ID this comment is scoped to. `null` for comments outside a sandbox environment. */
  sandbox: z.string().optional().describe("Sandbox ID this comment is scoped to. `null` for comments outside a sandbox environment."),
  /** ID of the task this comment belongs to (`tsk_...`). */
  task: z.string().optional().describe("ID of the task this comment belongs to (`tsk_...`)."),
  /** ID of the team the task belongs to (`tem_...`). `null` if not scoped to a team. */
  team: z.string().optional().describe("ID of the team the task belongs to (`tem_...`). `null` if not scoped to a team."),
  /** When this comment was last edited (ISO 8601). */
  updated_at: z.string().optional().describe("When this comment was last edited (ISO 8601)."),
}).describe("A comment posted on a task by a user or an agent, including resolved author information.") satisfies z.ZodType<TaskComment>;

/** A task-session lease returned only to its matching holder. */
export interface TaskSessionLease {
  /** Server-calculated lease expiry in ISO 8601 format. */
  expires_at: string;
  /** Bounded harness identifier for the matching coding session. */
  harness: string;
  /** Caller-generated fencing token required for renewal and release. */
  lease_id: string;
  /** Server timestamp for the most recent claim, reclaim, or renewal. */
  renewed_at: string;
  /** Opaque caller-generated coding-session identifier. */
  session_id: string;
  /** Display name supplied by the matching coding session. */
  session_name: string;
}

/** Runtime validator for {@link TaskSessionLease}. */
export const taskSessionLeaseSchema = z.object({
  /** Server-calculated lease expiry in ISO 8601 format. */
  expires_at: z.string().describe("Server-calculated lease expiry in ISO 8601 format."),
  /** Bounded harness identifier for the matching coding session. */
  harness: z.string().describe("Bounded harness identifier for the matching coding session."),
  /** Caller-generated fencing token required for renewal and release. */
  lease_id: z.string().describe("Caller-generated fencing token required for renewal and release."),
  /** Server timestamp for the most recent claim, reclaim, or renewal. */
  renewed_at: z.string().describe("Server timestamp for the most recent claim, reclaim, or renewal."),
  /** Opaque caller-generated coding-session identifier. */
  session_id: z.string().describe("Opaque caller-generated coding-session identifier."),
  /** Display name supplied by the matching coding session. */
  session_name: z.string().describe("Display name supplied by the matching coding session."),
}).describe("A task-session lease returned only to its matching holder.") satisfies z.ZodType<TaskSessionLease>;
