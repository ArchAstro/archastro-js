// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: bf397e5f8e0a

// Contract tests for the generated channel class. Drives the channel
// through a real WebSocket against the harness-service subprocess —
// the same service Python tests connect to. Scenario closures are
// serialized over an HTTP control API, not passed in-process.

import { describe, it, expect, beforeEach, afterEach } from "vitest";
import {
  ChannelJoinError,
  ChannelReplyError,
  HarnessServiceClient,
  type HarnessSocket,
} from "@archastro/channel-harness";
import { ApiChatChannel } from "../../../src/channels/api_chat_channel.js";

interface Rig {
  client: HarnessServiceClient;
  socket: HarnessSocket;
}

// Check the env vars inside bootHarness() rather than at module scope:
// a module-level throw would surface as a vitest collection error for the
// whole file, while a thrown inside bootHarness() becomes a normal test
// failure that doesn't tear down the rest of the suite.
async function bootHarness(): Promise<Rig> {
  const wsUrl = process.env.ARCHASTRO_HARNESS_WS_URL;
  const controlUrl = process.env.ARCHASTRO_HARNESS_CONTROL_URL;
  if (!wsUrl || !controlUrl) {
    throw new Error(
      "Channel contract tests require ARCHASTRO_HARNESS_WS_URL and " +
        "ARCHASTRO_HARNESS_CONTROL_URL — set by global-setup.ts."
    );
  }
  const client = new HarnessServiceClient({ wsUrl, controlUrl });
  await client.reset();
  const socket = await client.openSocket();
  return { client, socket };
}

function nextPush(
  subscribe: (cb: (payload: unknown) => void) => () => void,
  timeoutMs = 500
): Promise<unknown> {
  return new Promise((resolve, reject) => {
    let settled = false;
    let unsubscribe: (() => void) | null = null;
    const timer = setTimeout(() => {
      if (settled) return;
      settled = true;
      unsubscribe?.();
      reject(new Error("push not received in time"));
    }, timeoutMs);
    unsubscribe = subscribe((payload) => {
      if (settled) return;
      settled = true;
      clearTimeout(timer);
      unsubscribe?.();
      resolve(payload);
    });
    if (settled) unsubscribe();
  });
}

describe("ApiChatChannel.joinTeamThread (api:chat:team:{team_id}:thread:{thread_id})", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("joins and receives a contract-valid reply", async () => {
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    expect(channel).toBeInstanceOf(ApiChatChannel);
    expect(channel.joinResponse).toBeDefined();
  });

  it("surfaces an application-level error reply as ChannelJoinError", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "replyError", payload: { reason: "test_error" } }],
    });

    const err = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 }).catch((e: unknown) => e);
    expect(err).toBeInstanceOf(ChannelJoinError);
    expect((err as ChannelJoinError).response).toEqual({ reason: "test_error" });
  });
});

describe("ApiChatChannel.joinTeamKeyed (api:chat:team:{team_id}:key:{key})", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("joins and receives a contract-valid reply", async () => {
    const channel = await ApiChatChannel.joinTeamKeyed(rig.socket, "test-id", "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    expect(channel).toBeInstanceOf(ApiChatChannel);
    expect(channel.joinResponse).toBeDefined();
  });

  it("surfaces an application-level error reply as ChannelJoinError", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:key:test-key",
      onJoin: [{ type: "replyError", payload: { reason: "test_error" } }],
    });

    const err = await ApiChatChannel.joinTeamKeyed(rig.socket, "test-id", "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 }).catch((e: unknown) => e);
    expect(err).toBeInstanceOf(ChannelJoinError);
    expect((err as ChannelJoinError).response).toEqual({ reason: "test_error" });
  });
});

describe("ApiChatChannel.joinTeamTransient (api:chat:team:{team_id}:transient:{key})", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("joins and receives a contract-valid reply", async () => {
    const channel = await ApiChatChannel.joinTeamTransient(rig.socket, "test-id", "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    expect(channel).toBeInstanceOf(ApiChatChannel);
    expect(channel.joinResponse).toBeDefined();
  });

  it("surfaces an application-level error reply as ChannelJoinError", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:transient:test-key",
      onJoin: [{ type: "replyError", payload: { reason: "test_error" } }],
    });

    const err = await ApiChatChannel.joinTeamTransient(rig.socket, "test-id", "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 }).catch((e: unknown) => e);
    expect(err).toBeInstanceOf(ChannelJoinError);
    expect((err as ChannelJoinError).response).toEqual({ reason: "test_error" });
  });
});

describe("ApiChatChannel.joinUserThread (api:chat:user:thread:{thread_id})", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("joins and receives a contract-valid reply", async () => {
    const channel = await ApiChatChannel.joinUserThread(rig.socket, "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    expect(channel).toBeInstanceOf(ApiChatChannel);
    expect(channel.joinResponse).toBeDefined();
  });

  it("surfaces an application-level error reply as ChannelJoinError", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:user:thread:test-id",
      onJoin: [{ type: "replyError", payload: { reason: "test_error" } }],
    });

    const err = await ApiChatChannel.joinUserThread(rig.socket, "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 }).catch((e: unknown) => e);
    expect(err).toBeInstanceOf(ChannelJoinError);
    expect((err as ChannelJoinError).response).toEqual({ reason: "test_error" });
  });
});

describe("ApiChatChannel.joinUserKeyed (api:chat:user:key:{key})", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("joins and receives a contract-valid reply", async () => {
    const channel = await ApiChatChannel.joinUserKeyed(rig.socket, "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    expect(channel).toBeInstanceOf(ApiChatChannel);
    expect(channel.joinResponse).toBeDefined();
  });

  it("surfaces an application-level error reply as ChannelJoinError", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:user:key:test-key",
      onJoin: [{ type: "replyError", payload: { reason: "test_error" } }],
    });

    const err = await ApiChatChannel.joinUserKeyed(rig.socket, "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 }).catch((e: unknown) => e);
    expect(err).toBeInstanceOf(ChannelJoinError);
    expect((err as ChannelJoinError).response).toEqual({ reason: "test_error" });
  });
});

describe("ApiChatChannel.joinUserTransient (api:chat:user:transient:{key})", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("joins and receives a contract-valid reply", async () => {
    const channel = await ApiChatChannel.joinUserTransient(rig.socket, "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    expect(channel).toBeInstanceOf(ApiChatChannel);
    expect(channel.joinResponse).toBeDefined();
  });

  it("surfaces an application-level error reply as ChannelJoinError", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:user:transient:test-key",
      onJoin: [{ type: "replyError", payload: { reason: "test_error" } }],
    });

    const err = await ApiChatChannel.joinUserTransient(rig.socket, "test-key", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 }).catch((e: unknown) => e);
    expect(err).toBeInstanceOf(ChannelJoinError);
    expect((err as ChannelJoinError).response).toEqual({ reason: "test_error" });
  });
});

describe("ApiChatChannel.apiChatForkThread (api:chat:fork_thread)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:fork_thread": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatForkThread({ message_id: "test-id", title: "test-value" });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:fork_thread");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toEqual(expect.objectContaining({ message_id: "test-id" }));
  });

  it("rejects with ChannelReplyError when required params are missing", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });

    await expect(channel.apiChatForkThread({})).rejects.toBeInstanceOf(ChannelReplyError);
  });
});

describe("ApiChatChannel.apiChatMarkThreadRead (api:chat:mark_thread_read)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:mark_thread_read": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatMarkThreadRead({ message_id: "test-id" });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:mark_thread_read");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toEqual(expect.objectContaining({ message_id: "test-id" }));
  });

  it("rejects with ChannelReplyError when required params are missing", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });

    await expect(channel.apiChatMarkThreadRead({})).rejects.toBeInstanceOf(ChannelReplyError);
  });
});

describe("ApiChatChannel.apiChatListMessages (api:chat:list_messages)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:list_messages": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatListMessages({});
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:list_messages");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toBeDefined();
  });
});

describe("ApiChatChannel.apiChatLoadMoreMessages (api:chat:load_more_messages)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:load_more_messages": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatLoadMoreMessages({ after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:load_more_messages");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toBeDefined();
  });
});

describe("ApiChatChannel.apiChatPostMessage (api:chat:post_message)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:post_message": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatPostMessage({ content: "test content", idempotency_key: "test-key", reply_to: "test-value", uploads: [{}] });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:post_message");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toEqual(expect.objectContaining({ content: "test content" }));
  });

  it("rejects with ChannelReplyError when required params are missing", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });

    await expect(channel.apiChatPostMessage({})).rejects.toBeInstanceOf(ChannelReplyError);
  });
});

describe("ApiChatChannel.apiChatPostSimpleMessage (api:chat:post_simple_message)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:post_simple_message": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatPostSimpleMessage({ content: "test content", idempotency_key: "test-key", reply_to: "test-value" });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:post_simple_message");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toBeDefined();
  });
});

describe("ApiChatChannel.apiChatEditMessage (api:chat:edit_message)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:edit_message": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatEditMessage({ content: "test content", message_id: "test-id" });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:edit_message");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toEqual(expect.objectContaining({ content: "test content", message_id: "test-id" }));
  });

  it("rejects with ChannelReplyError when required params are missing", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });

    await expect(channel.apiChatEditMessage({})).rejects.toBeInstanceOf(ChannelReplyError);
  });
});

describe("ApiChatChannel.apiChatDeleteMessage (api:chat:delete_message)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:delete_message": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatDeleteMessage({ message_id: "test-id" });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:delete_message");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toEqual(expect.objectContaining({ message_id: "test-id" }));
  });

  it("rejects with ChannelReplyError when required params are missing", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });

    await expect(channel.apiChatDeleteMessage({})).rejects.toBeInstanceOf(ChannelReplyError);
  });
});

describe("ApiChatChannel.apiChatAddReaction (api:chat:add_reaction)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:add_reaction": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatAddReaction({ emoji: "test-value", message_id: "test-id" });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:add_reaction");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toEqual(expect.objectContaining({ emoji: "test-value", message_id: "test-id" }));
  });

  it("rejects with ChannelReplyError when required params are missing", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });

    await expect(channel.apiChatAddReaction({})).rejects.toBeInstanceOf(ChannelReplyError);
  });
});

describe("ApiChatChannel.apiChatRemoveReaction (api:chat:remove_reaction)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("sends a valid push and receives a contract-valid reply", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
      onMessage: {
        "api:chat:remove_reaction": [{ type: "autoReply" }],
      },
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const reply = await channel.apiChatRemoveReaction({ emoji: "test-value", message_id: "test-id" });
    expect(reply).toBeDefined();

    const observed = await rig.client.observations("api:chat:team:test-id:thread:test-id", "api:chat:remove_reaction");
    expect(observed).toHaveLength(1);
    expect(observed[0]!.params).toEqual(expect.objectContaining({ emoji: "test-value", message_id: "test-id" }));
  });

  it("rejects with ChannelReplyError when required params are missing", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });

    await expect(channel.apiChatRemoveReaction({})).rejects.toBeInstanceOf(ChannelReplyError);
  });
});

describe("ApiChatChannel.onMessageAdded (message_added)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("delivers contract-valid payloads to onMessageAdded", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [
        { type: "autoReply" },
        { type: "autoPush", event: "message_added" },
      ],
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const payload = await nextPush((cb) => channel.onMessageAdded(cb));
    expect(payload).toBeDefined();
  });
});

describe("ApiChatChannel.onMessageUpdated (message_updated)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("delivers contract-valid payloads to onMessageUpdated", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [
        { type: "autoReply" },
        { type: "autoPush", event: "message_updated" },
      ],
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const payload = await nextPush((cb) => channel.onMessageUpdated(cb));
    expect(payload).toBeDefined();
  });
});

describe("ApiChatChannel.onThreadEvent (thread_event)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("delivers contract-valid payloads to onThreadEvent", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [
        { type: "autoReply" },
        { type: "autoPush", event: "thread_event" },
      ],
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const payload = await nextPush((cb) => channel.onThreadEvent(cb));
    expect(payload).toBeDefined();
  });
});

describe("ApiChatChannel.onSystemEvent (system_event)", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("delivers contract-valid payloads to onSystemEvent", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [
        { type: "autoReply" },
        { type: "autoPush", event: "system_event" },
      ],
    });

    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    const payload = await nextPush((cb) => channel.onSystemEvent(cb));
    expect(payload).toBeDefined();
  });
});

describe("ApiChatChannel.leave", () => {
  let rig: Rig;
  beforeEach(async () => {
    rig = await bootHarness();
  });
  afterEach(() => {
    rig.client.closeAllSockets();
  });

  it("leaves cleanly through the generated leave()", async () => {
    await rig.client.registerScenario({
      topic: "api:chat:team:test-id:thread:test-id",
      onJoin: [{ type: "autoReply" }],
    });
    const channel = await ApiChatChannel.joinTeamThread(rig.socket, "test-id", "test-id", { after_cursor: "test-value", before_cursor: "test-value", include_metadata: true, limit: 1 });
    await channel.leave();
  });
});

