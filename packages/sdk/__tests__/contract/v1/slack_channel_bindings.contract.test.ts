// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 4aea0b4b5e33


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: slack_channel_bindings", () => {
  describe("slack_channel_bindings", () => {
    it("GET /api/v1/slack_channel_bindings returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/slack_channel_bindings returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/slack_channel_bindings returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/slack_channel_bindings returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/slack_channel_bindings returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.create({ agent_user_ids: ["test-value"], channel_id: "test-id", slack_team_id: "test-id", team_id: "test-id" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/slack_channel_bindings returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.create({ agent_user_ids: ["test-value"], channel_id: "test-id", slack_team_id: "test-id", team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/slack_channel_bindings returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.create({ agent_user_ids: ["test-value"], channel_id: "test-id", slack_team_id: "test-id", team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/slack_channel_bindings returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.create({ agent_user_ids: ["test-value"], channel_id: "test-id", slack_team_id: "test-id", team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/slack_channel_bindings returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.slack_channel_bindings.create({ agent_user_ids: ["test-value"], channel_id: "test-id", slack_team_id: "test-id", team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/slack_channel_bindings returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.slack_channel_bindings.create({ agent_user_ids: ["test-value"], channel_id: "test-id", slack_team_id: "test-id", team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/slack_channel_bindings returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.slack_channel_bindings.create({ agent_user_ids: ["test-value"], channel_id: "test-id", slack_team_id: "test-id", team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/slack_channel_bindings/assign returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.assign({ agent_user_id: "test-id", channel_id: "test-id", slack_team_id: "test-id" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/slack_channel_bindings/assign returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.assign({ agent_user_id: "test-id", channel_id: "test-id", slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/slack_channel_bindings/assign returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.assign({ agent_user_id: "test-id", channel_id: "test-id", slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/slack_channel_bindings/assign returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.assign({ agent_user_id: "test-id", channel_id: "test-id", slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/slack_channel_bindings/assign returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.slack_channel_bindings.assign({ agent_user_id: "test-id", channel_id: "test-id", slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/slack_channel_bindings/assign returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.slack_channel_bindings.assign({ agent_user_id: "test-id", channel_id: "test-id", slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/slack_channel_bindings/assign returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.slack_channel_bindings.assign({ agent_user_id: "test-id", channel_id: "test-id", slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 402", async () => {
      const ec = errorClient(402);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(402);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 500", async () => {
      const ec = errorClient(500);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(500);
      }
    });

    it("POST /api/v1/slack_channel_bindings/provision returns 502", async () => {
      const ec = errorClient(502);
      try {
        await ec.v1.slack_channel_bindings.provision({ customer_key: "test-key", customer_label: "test-value", slack_team_id: "test-id", template_config_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(502);
      }
    });

    it("DELETE /api/v1/slack_channel_bindings/{channel} returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.delete("test-value");
      expect(result).toBeDefined();
    });

    it("DELETE /api/v1/slack_channel_bindings/{channel} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("DELETE /api/v1/slack_channel_bindings/{channel} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/slack_channel_bindings/{channel} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/slack_channel_bindings/{channel} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.slack_channel_bindings.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/slack_channel_bindings/{channel} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.slack_channel_bindings.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel} returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.get("test-value", { slackTeamId: "test-id" });
      expect(result).toBeDefined();
    });

    it("GET /api/v1/slack_channel_bindings/{channel} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.get("test-value", { slackTeamId: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.get("test-value", { slackTeamId: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.get("test-value", { slackTeamId: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.slack_channel_bindings.get("test-value", { slackTeamId: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.slack_channel_bindings.get("test-value", { slackTeamId: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel}/delivery_outcomes returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.deliveryOutcomes("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/slack_channel_bindings/{channel}/delivery_outcomes returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.deliveryOutcomes("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel}/delivery_outcomes returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.deliveryOutcomes("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel}/delivery_outcomes returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.deliveryOutcomes("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/slack_channel_bindings/{channel}/delivery_outcomes returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.slack_channel_bindings.deliveryOutcomes("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/slack_channel_bindings/{channel}/deposit_thread returns successfully", async () => {
      const result = await client.v1.slack_channel_bindings.depositThread("test-value", { slack_team_id: "test-id" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/slack_channel_bindings/{channel}/deposit_thread returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.slack_channel_bindings.depositThread("test-value", { slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/slack_channel_bindings/{channel}/deposit_thread returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.slack_channel_bindings.depositThread("test-value", { slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/slack_channel_bindings/{channel}/deposit_thread returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.slack_channel_bindings.depositThread("test-value", { slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/slack_channel_bindings/{channel}/deposit_thread returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.slack_channel_bindings.depositThread("test-value", { slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/slack_channel_bindings/{channel}/deposit_thread returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.slack_channel_bindings.depositThread("test-value", { slack_team_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
