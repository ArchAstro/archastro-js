// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: daa9e9163605


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: solutions", () => {
  describe("solutions", () => {
    it("GET /api/v1/solutions returns successfully", async () => {
      const result = await client.v1.solutions.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/solutions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.solutions.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/solutions returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.solutions.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/solutions returns successfully", async () => {
      const result = await client.v1.solutions.create({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/solutions returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.solutions.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/solutions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.solutions.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/solutions returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.solutions.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/solutions returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.solutions.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/solutions returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.solutions.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/solutions returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.solutions.create({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/solutions/{solution} returns successfully", async () => {
      const result = await client.v1.solutions.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/solutions/{solution} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.solutions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/solutions/{solution} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.solutions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/solutions/{solution} returns 500", async () => {
      const ec = errorClient(500);
      try {
        await ec.v1.solutions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(500);
      }
    });

    it("GET /api/v1/solutions/{solution} returns successfully", async () => {
      const result = await client.v1.solutions.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/solutions/{solution} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.solutions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/solutions/{solution} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.solutions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/solutions/{solution} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.solutions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/solutions/{solution}/dependents returns successfully", async () => {
      const result = await client.v1.solutions.dependents("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/solutions/{solution}/dependents returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.solutions.dependents("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/solutions/{solution}/dependents returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.solutions.dependents("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/solutions/{solution}/install returns successfully", async () => {
      const result = await client.v1.solutions.install("test-value", {});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/solutions/{solution}/install returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.solutions.install("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/solutions/{solution}/install returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.solutions.install("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/solutions/{solution}/install returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.solutions.install("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/solutions/{solution}/install returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.solutions.install("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/solutions/{solution}/install returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.solutions.install("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/solutions/{solution}/install returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.solutions.install("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/solutions/{solution}/readme returns successfully", async () => {
      const result = await client.v1.solutions.readme("test-value", { token: "test-token" });
      expect(result.content).toBeDefined();
      expect(result.mimeType).toBeTruthy();
    });

    it("GET /api/v1/solutions/{solution}/readme returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.solutions.readme("test-value", { token: "test-token" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/solutions/{solution}/readme returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.solutions.readme("test-value", { token: "test-token" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/solutions/{solution}/upgrade returns successfully", async () => {
      const result = await client.v1.solutions.upgrade("test-value", {});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/solutions/{solution}/upgrade returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.solutions.upgrade("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/solutions/{solution}/upgrade returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.solutions.upgrade("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/solutions/{solution}/upgrade returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.solutions.upgrade("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/solutions/{solution}/upgrade returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.solutions.upgrade("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/solutions/{solution}/upgrade returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.solutions.upgrade("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
