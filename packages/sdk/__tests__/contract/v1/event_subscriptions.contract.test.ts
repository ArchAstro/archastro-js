// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 996380e20859


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: event_subscriptions", () => {
  describe("event_subscriptions", () => {
    it("GET /api/v1/event_subscriptions returns successfully", async () => {
      const result = await client.v1.event_subscriptions.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/event_subscriptions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/event_subscriptions returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.event_subscriptions.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/event_subscriptions returns successfully", async () => {
      const result = await client.v1.event_subscriptions.create({ event_names: ["test-value"], name: "test-name" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/event_subscriptions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.create({ event_names: ["test-value"], name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/event_subscriptions returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.event_subscriptions.create({ event_names: ["test-value"], name: "test-name" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/event_subscriptions/{subscription} returns successfully", async () => {
      const result = await client.v1.event_subscriptions.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/event_subscriptions/{subscription} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/event_subscriptions/{subscription} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.event_subscriptions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/event_subscriptions/{subscription} returns successfully", async () => {
      const result = await client.v1.event_subscriptions.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/event_subscriptions/{subscription} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/event_subscriptions/{subscription} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.event_subscriptions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/event_subscriptions/{subscription} returns successfully", async () => {
      const result = await client.v1.event_subscriptions.update("test-value", {});
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/event_subscriptions/{subscription} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.update("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/event_subscriptions/{subscription} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.event_subscriptions.update("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/event_subscriptions/{subscription} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.event_subscriptions.update("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/event_subscriptions/{subscription}/claim returns successfully", async () => {
      const result = await client.v1.event_subscriptions.claim("test-value", {});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/event_subscriptions/{subscription}/claim returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.claim("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/event_subscriptions/{subscription}/claim returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.event_subscriptions.claim("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/event_subscriptions/{subscription}/claim returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.event_subscriptions.claim("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/event_subscriptions/{subscription}/head returns successfully", async () => {
      const result = await client.v1.event_subscriptions.head("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/event_subscriptions/{subscription}/head returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.head("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/event_subscriptions/{subscription}/head returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.event_subscriptions.head("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/event_subscriptions/{subscription}/queue returns successfully", async () => {
      const result = await client.v1.event_subscriptions.queue("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/event_subscriptions/{subscription}/queue returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.event_subscriptions.queue("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/event_subscriptions/{subscription}/queue returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.event_subscriptions.queue("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/event_subscriptions/{subscription}/queue returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.event_subscriptions.queue("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

});
