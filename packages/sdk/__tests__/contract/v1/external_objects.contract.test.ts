// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 727e4c08f199


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: external_objects", () => {
  describe("external_objects", () => {
    it("GET /api/v1/external_objects returns successfully", async () => {
      const result = await client.v1.external_objects.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/external_objects returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.external_objects.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/external_objects returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.external_objects.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/external_objects returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.external_objects.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/external_objects returns successfully", async () => {
      const result = await client.v1.external_objects.create({ object: { name: "test-name", type: "r2_bucket" }, service: "cloudflare" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/external_objects returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.external_objects.create({ object: { name: "test-name", type: "r2_bucket" }, service: "cloudflare" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/external_objects returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.external_objects.create({ object: { name: "test-name", type: "r2_bucket" }, service: "cloudflare" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/external_objects returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.external_objects.create({ object: { name: "test-name", type: "r2_bucket" }, service: "cloudflare" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/external_objects returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.external_objects.create({ object: { name: "test-name", type: "r2_bucket" }, service: "cloudflare" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/external_objects returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.external_objects.create({ object: { name: "test-name", type: "r2_bucket" }, service: "cloudflare" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/external_objects returns 502", async () => {
      const ec = errorClient(502);
      try {
        await ec.v1.external_objects.create({ object: { name: "test-name", type: "r2_bucket" }, service: "cloudflare" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(502);
      }
    });

    it("DELETE /api/v1/external_objects/{external_object} returns successfully", async () => {
      const result = await client.v1.external_objects.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/external_objects/{external_object} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.external_objects.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/external_objects/{external_object} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.external_objects.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/external_objects/{external_object} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.external_objects.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/external_objects/{external_object} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.external_objects.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/external_objects/{external_object} returns 502", async () => {
      const ec = errorClient(502);
      try {
        await ec.v1.external_objects.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(502);
      }
    });

    it("GET /api/v1/external_objects/{external_object} returns successfully", async () => {
      const result = await client.v1.external_objects.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/external_objects/{external_object} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.external_objects.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/external_objects/{external_object} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.external_objects.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/external_objects/{external_object} returns successfully", async () => {
      const result = await client.v1.external_objects.update("test-value", { object: { storage_class: "test-value", type: "r2_bucket" } });
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/external_objects/{external_object} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.external_objects.update("test-value", { object: { storage_class: "test-value", type: "r2_bucket" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/external_objects/{external_object} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.external_objects.update("test-value", { object: { storage_class: "test-value", type: "r2_bucket" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PATCH /api/v1/external_objects/{external_object} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.external_objects.update("test-value", { object: { storage_class: "test-value", type: "r2_bucket" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/external_objects/{external_object} returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.external_objects.update("test-value", { object: { storage_class: "test-value", type: "r2_bucket" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("PATCH /api/v1/external_objects/{external_object} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.external_objects.update("test-value", { object: { storage_class: "test-value", type: "r2_bucket" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("PATCH /api/v1/external_objects/{external_object} returns 502", async () => {
      const ec = errorClient(502);
      try {
        await ec.v1.external_objects.update("test-value", { object: { storage_class: "test-value", type: "r2_bucket" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(502);
      }
    });

  });

});
