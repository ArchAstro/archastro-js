// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 97e6afac898a


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: artifacts", () => {
  describe("artifacts", () => {
    it("DELETE /api/v1/artifacts/{artifact} returns successfully", async () => {
      const result = await client.v1.artifacts.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/artifacts/{artifact} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.artifacts.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/artifacts/{artifact} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.artifacts.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/artifacts/{artifact} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.artifacts.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/artifacts/{artifact} returns successfully", async () => {
      const result = await client.v1.artifacts.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/artifacts/{artifact} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.artifacts.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/artifacts/{artifact} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.artifacts.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/artifacts/{artifact} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.artifacts.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/artifacts/{artifact} returns successfully", async () => {
      const result = await client.v1.artifacts.replace("test-value", { from_version: 1 });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/artifacts/{artifact} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.artifacts.replace("test-value", { from_version: 1 });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/artifacts/{artifact} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.artifacts.replace("test-value", { from_version: 1 });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PUT /api/v1/artifacts/{artifact} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.artifacts.replace("test-value", { from_version: 1 });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/artifacts/{artifact} returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.artifacts.replace("test-value", { from_version: 1 });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("PUT /api/v1/artifacts/{artifact} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.artifacts.replace("test-value", { from_version: 1 });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/artifacts/{artifact}/archive returns successfully", async () => {
      const result = await client.v1.artifacts.archive("test-value");
      expect(result).toBeUndefined();
    });

    it("POST /api/v1/artifacts/{artifact}/archive returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.artifacts.archive("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/artifacts/{artifact}/archive returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.artifacts.archive("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/artifacts/{artifact}/archive returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.artifacts.archive("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/artifacts/{artifact}/content returns successfully", async () => {
      const result = await client.v1.artifacts.content("test-value");
      expect(result.content).toBeDefined();
      expect(result.mimeType).toBeTruthy();
    });

    it("GET /api/v1/artifacts/{artifact}/content returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.artifacts.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/artifacts/{artifact}/content returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.artifacts.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/artifacts/{artifact}/content returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.artifacts.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/artifacts/{artifact}/content returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.artifacts.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
