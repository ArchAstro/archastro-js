// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: ff4ec2da4bb5


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: private_service_definitions", () => {
  describe("private_service_definitions", () => {
    it("GET /api/v1/private_service_definitions/{app_id}/{private_service_id} returns successfully", async () => {
      const result = await client.v1.private_service_definitions.get("test-id", "test-id");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/private_service_definitions/{app_id}/{private_service_id} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.private_service_definitions.get("test-id", "test-id");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/private_service_definitions/{app_id}/{private_service_id} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.private_service_definitions.get("test-id", "test-id");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/private_service_definitions/{app_id}/{private_service_id} returns 503", async () => {
      const ec = errorClient(503);
      try {
        await ec.v1.private_service_definitions.get("test-id", "test-id");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(503);
      }
    });

  });

});
