// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: cd8c2111d849


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: agent_computers", () => {
  describe("agent_computers", () => {
    it("DELETE /api/v1/agent_computers/{computer} returns successfully", async () => {
      const result = await client.v1.agent_computers.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/agent_computers/{computer} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_computers.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/agent_computers/{computer} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_computers.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/agent_computers/{computer} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_computers.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agent_computers/{computer} returns successfully", async () => {
      const result = await client.v1.agent_computers.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_computers/{computer} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_computers.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_computers/{computer} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_computers.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_computers/{computer} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_computers.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_computers/{computer}/exec returns successfully", async () => {
      const result = await client.v1.agent_computers.exec("test-value", { command: "test-value", computer: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_computers/{computer}/exec returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_computers.exec("test-value", { command: "test-value", computer: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_computers/{computer}/exec returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_computers.exec("test-value", { command: "test-value", computer: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_computers/{computer}/exec returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_computers.exec("test-value", { command: "test-value", computer: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_computers/{computer}/exec returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_computers.exec("test-value", { command: "test-value", computer: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_computers/{computer}/refresh returns successfully", async () => {
      const result = await client.v1.agent_computers.refresh("test-value", { computer: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_computers/{computer}/refresh returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_computers.refresh("test-value", { computer: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_computers/{computer}/refresh returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_computers.refresh("test-value", { computer: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_computers/{computer}/refresh returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_computers.refresh("test-value", { computer: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

});
