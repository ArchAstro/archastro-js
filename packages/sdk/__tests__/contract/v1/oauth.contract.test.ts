// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 49e4fc3bd881


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: oauth", () => {
  describe("oauth", () => {
    it("GET /oauth/scopes returns successfully", async () => {
      const result = await client.v1.oauth.scopes();
      expect(result).toBeDefined();
    });

    it("POST /oauth/token returns successfully", async () => {
      const result = await client.v1.oauth.token({ grant_type: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /oauth/token returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.oauth.token({ grant_type: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /oauth/token returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.oauth.token({ grant_type: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /oauth/token returns 429", async () => {
      const ec = errorClient(429);
      try {
        await ec.v1.oauth.token({ grant_type: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(429);
      }
    });

  });

  describe("oauth > device", () => {
    it("POST /oauth/device/approve returns successfully", async () => {
      const result = await client.v1.oauth.device.approve({ user_code: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /oauth/device/approve returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.oauth.device.approve({ user_code: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /oauth/device/approve returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.oauth.device.approve({ user_code: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /oauth/device/authorization returns successfully", async () => {
      const result = await client.v1.oauth.device.authorization({ code: "test-value" });
      expect(result).toBeDefined();
    });

    it("GET /oauth/device/authorization returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.oauth.device.authorization({ code: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /oauth/device/authorization returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.oauth.device.authorization({ code: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /oauth/device/authorize returns successfully", async () => {
      const result = await client.v1.oauth.device.authorize({ client: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /oauth/device/authorize returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.oauth.device.authorize({ client: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /oauth/device/authorize returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.oauth.device.authorize({ client: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /oauth/device/authorize returns 429", async () => {
      const ec = errorClient(429);
      try {
        await ec.v1.oauth.device.authorize({ client: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(429);
      }
    });

    it("POST /oauth/device/deny returns successfully", async () => {
      const result = await client.v1.oauth.device.deny({ user_code: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /oauth/device/deny returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.oauth.device.deny({ user_code: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /oauth/device/deny returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.oauth.device.deny({ user_code: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

});
