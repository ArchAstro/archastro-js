// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: eec8c1f7e09b


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: workflows", () => {
  describe("workflows", () => {
    it("GET /api/v1/workflows/commands returns successfully", async () => {
      const result = await client.v1.workflows.commands();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/workflows/commands returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.commands();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/workflows/llm.txt returns successfully", async () => {
      const result = await client.v1.workflows.llmTxt();
      expect(result.content).toBeDefined();
      expect(result.mimeType).toBeTruthy();
    });

    it("GET /api/v1/workflows/llm.txt returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.llmTxt();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/workflows/run returns successfully", async () => {
      const result = await client.v1.workflows.run({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/workflows/run returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.run({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/workflows/run returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.workflows.run({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("workflows > events", () => {
    it("GET /api/v1/workflows/events/{name}/sample returns successfully", async () => {
      const result = await client.v1.workflows.events.sample("test-name");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/workflows/events/{name}/sample returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.events.sample("test-name");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/workflows/events/{name}/sample returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.workflows.events.sample("test-name");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("workflows > expressions", () => {
    it("POST /api/v1/workflows/expressions/run returns successfully", async () => {
      const result = await client.v1.workflows.expressions.run({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/workflows/expressions/run returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.expressions.run({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/workflows/expressions/run returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.workflows.expressions.run({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/workflows/expressions/validate returns successfully", async () => {
      const result = await client.v1.workflows.expressions.validate({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/workflows/expressions/validate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.expressions.validate({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

  describe("workflows > graph", () => {
    it("POST /api/v1/workflows/graph/validate returns successfully", async () => {
      const result = await client.v1.workflows.graph.validate({ graph: {} });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/workflows/graph/validate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.graph.validate({ graph: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

  describe("workflows > node_types", () => {
    it("GET /api/v1/workflows/node_types returns successfully", async () => {
      const result = await client.v1.workflows.node_types.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/workflows/node_types returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.node_types.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/workflows/node_types/{node_type} returns successfully", async () => {
      const result = await client.v1.workflows.node_types.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/workflows/node_types/{node_type} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.node_types.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/workflows/node_types/{node_type} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.workflows.node_types.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("workflows > sample", () => {
    it("GET /api/v1/workflows/sample returns successfully", async () => {
      const result = await client.v1.workflows.sample.list();
      expect(result.content).toBeDefined();
      expect(result.mimeType).toBeTruthy();
    });

    it("GET /api/v1/workflows/sample returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.workflows.sample.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

});
