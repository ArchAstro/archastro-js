// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: bdf343c2b204


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: scripts", () => {
  describe("scripts", () => {
    it("GET /api/v1/scripts/language returns successfully", async () => {
      const result = await client.v1.scripts.language();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/scripts/language returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.scripts.language();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/scripts/llm.txt returns successfully", async () => {
      const result = await client.v1.scripts.llmTxt();
      expect(result.content).toBeDefined();
      expect(result.mimeType).toBeTruthy();
    });

    it("GET /api/v1/scripts/llm.txt returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.scripts.llmTxt();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/scripts/run returns successfully", async () => {
      const result = await client.v1.scripts.run({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/scripts/run returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.scripts.run({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/scripts/run returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.scripts.run({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/scripts/runtime_env_vars returns successfully", async () => {
      const result = await client.v1.scripts.runtimeEnvVars();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/scripts/runtime_env_vars returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.scripts.runtimeEnvVars();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/scripts/runtime_env_vars returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.scripts.runtimeEnvVars();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/scripts/test returns successfully", async () => {
      const result = await client.v1.scripts.test({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/scripts/test returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.scripts.test({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/scripts/test returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.scripts.test({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/scripts/validate returns successfully", async () => {
      const result = await client.v1.scripts.validate({});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/scripts/validate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.scripts.validate({});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

  });

});
