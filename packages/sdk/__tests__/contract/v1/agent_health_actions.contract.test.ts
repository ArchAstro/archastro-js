// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 5f2f24ebd6df


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: agent_health_actions", () => {
  describe("agent_health_actions", () => {
    it("GET /api/v1/agent_health_actions/{health_action} returns successfully", async () => {
      const result = await client.v1.agent_health_actions.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_health_actions/{health_action} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_health_actions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_health_actions/{health_action} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_health_actions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_health_actions/{health_action} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_health_actions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_health_actions/{health_action}/verify returns successfully", async () => {
      const result = await client.v1.agent_health_actions.verify("test-value");
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_health_actions/{health_action}/verify returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_health_actions.verify("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_health_actions/{health_action}/verify returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_health_actions.verify("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_health_actions/{health_action}/verify returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_health_actions.verify("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_health_actions/{health_action}/verify returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_health_actions.verify("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
