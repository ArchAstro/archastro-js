// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 6f0ca93e5f8b


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: sandboxes", () => {
  describe("sandboxes", () => {
    it("POST /api/v1/sandboxes returns successfully", async () => {
      const result = await client.v1.sandboxes.create({ name: "test-name", slug: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/sandboxes returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.sandboxes.create({ name: "test-name", slug: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/sandboxes returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.sandboxes.create({ name: "test-name", slug: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/sandboxes returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.sandboxes.create({ name: "test-name", slug: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/sandboxes returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.sandboxes.create({ name: "test-name", slug: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/sandboxes/{sandbox} returns successfully", async () => {
      const result = await client.v1.sandboxes.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/sandboxes/{sandbox} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.sandboxes.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/sandboxes/{sandbox} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.sandboxes.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/sandboxes/{sandbox} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.sandboxes.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/sandboxes/{sandbox} returns successfully", async () => {
      const result = await client.v1.sandboxes.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/sandboxes/{sandbox} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.sandboxes.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/sandboxes/{sandbox} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.sandboxes.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/sandboxes/{sandbox} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.sandboxes.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/sandboxes/{sandbox}/keys returns successfully", async () => {
      const result = await client.v1.sandboxes.keys("test-value", {});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/sandboxes/{sandbox}/keys returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.sandboxes.keys("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/sandboxes/{sandbox}/keys returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.sandboxes.keys("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/sandboxes/{sandbox}/keys returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.sandboxes.keys("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/sandboxes/{sandbox}/keys returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.sandboxes.keys("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
