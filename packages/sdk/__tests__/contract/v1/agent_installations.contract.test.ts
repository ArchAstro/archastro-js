// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 6b9cbd5b23d2


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: agent_installations", () => {
  describe("agent_installations", () => {
    it("GET /api/v1/agent_installations returns successfully", async () => {
      const result = await client.v1.agent_installations.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_installations returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_installations returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/agent_installations/{installation} returns successfully", async () => {
      const result = await client.v1.agent_installations.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/agent_installations/{installation} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/agent_installations/{installation} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/agent_installations/{installation} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_installations.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agent_installations/{installation} returns successfully", async () => {
      const result = await client.v1.agent_installations.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_installations/{installation} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_installations/{installation} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_installations/{installation} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_installations.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/activate returns successfully", async () => {
      const result = await client.v1.agent_installations.activate("test-value");
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_installations/{installation}/activate returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.activate("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/activate returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.activate("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/activate returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_installations.activate("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/activate returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_installations.activate("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/pause returns successfully", async () => {
      const result = await client.v1.agent_installations.pause("test-value");
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_installations/{installation}/pause returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.pause("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/pause returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.pause("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/pause returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_installations.pause("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/pause returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_installations.pause("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/suspend returns successfully", async () => {
      const result = await client.v1.agent_installations.suspend("test-value", {});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_installations/{installation}/suspend returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.suspend("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/suspend returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.suspend("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/suspend returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_installations.suspend("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/suspend returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_installations.suspend("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("agent_installations > installation_sources", () => {
    it("GET /api/v1/agent_installations/{installation}/installation_sources returns successfully", async () => {
      const result = await client.v1.agent_installations.installation_sources.list("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_installations/{installation}/installation_sources returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.installation_sources.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_installations/{installation}/installation_sources returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.installation_sources.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_installations/{installation}/installation_sources returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_installations.installation_sources.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/installation_sources returns successfully", async () => {
      const result = await client.v1.agent_installations.installation_sources.create("test-value", { payload: {}, type: "test" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_installations/{installation}/installation_sources returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_installations.installation_sources.create("test-value", { payload: {}, type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/installation_sources returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_installations.installation_sources.create("test-value", { payload: {}, type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/installation_sources returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_installations.installation_sources.create("test-value", { payload: {}, type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_installations/{installation}/installation_sources returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_installations.installation_sources.create("test-value", { payload: {}, type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
