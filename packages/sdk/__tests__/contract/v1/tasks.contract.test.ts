// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 8c636a33ce50


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: tasks", () => {
  describe("tasks", () => {
    it("DELETE /api/v1/tasks/{task} returns successfully", async () => {
      const result = await client.v1.tasks.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/tasks/{task} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/tasks/{task} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/tasks/{task} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/tasks/{task} returns successfully", async () => {
      const result = await client.v1.tasks.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/tasks/{task} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/tasks/{task} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/tasks/{task} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("PUT /api/v1/tasks/{task} returns successfully", async () => {
      const result = await client.v1.tasks.replace("test-value", {});
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/tasks/{task} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.replace("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/tasks/{task} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.tasks.replace("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PUT /api/v1/tasks/{task} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.replace("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/tasks/{task} returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.tasks.replace("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("PUT /api/v1/tasks/{task} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.replace("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/tasks/{task}/activity returns successfully", async () => {
      const result = await client.v1.tasks.activity("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/tasks/{task}/activity returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.activity("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/tasks/{task}/activity returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.activity("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/tasks/{task}/activity returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.activity("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/tasks/{task}/blocking returns successfully", async () => {
      const result = await client.v1.tasks.blocking("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/tasks/{task}/blocking returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.blocking("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/tasks/{task}/blocking returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.blocking("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/tasks/{task}/blocking returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.blocking("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/tasks/{task}/subtasks returns successfully", async () => {
      const result = await client.v1.tasks.subtasks("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/tasks/{task}/subtasks returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.subtasks("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/tasks/{task}/subtasks returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.subtasks("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/tasks/{task}/subtasks returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.subtasks("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("tasks > blockers", () => {
    it("GET /api/v1/tasks/{task}/blockers returns successfully", async () => {
      const result = await client.v1.tasks.blockers.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/tasks/{task}/blockers returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.blockers.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/tasks/{task}/blockers returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.blockers.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/tasks/{task}/blockers returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.blockers.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/tasks/{task}/blockers returns successfully", async () => {
      const result = await client.v1.tasks.blockers.create("test-value", { blocker: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/tasks/{task}/blockers returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.blockers.create("test-value", { blocker: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/tasks/{task}/blockers returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.blockers.create("test-value", { blocker: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/tasks/{task}/blockers returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.blockers.create("test-value", { blocker: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/tasks/{task}/blockers/{blocker} returns successfully", async () => {
      const result = await client.v1.tasks.blockers.delete("test-value", "test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/tasks/{task}/blockers/{blocker} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.blockers.delete("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/tasks/{task}/blockers/{blocker} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.blockers.delete("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/tasks/{task}/blockers/{blocker} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.blockers.delete("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("tasks > comments", () => {
    it("GET /api/v1/tasks/{task}/comments returns successfully", async () => {
      const result = await client.v1.tasks.comments.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/tasks/{task}/comments returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.comments.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/tasks/{task}/comments returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.comments.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/tasks/{task}/comments returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.comments.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/tasks/{task}/comments returns successfully", async () => {
      const result = await client.v1.tasks.comments.create("test-value", { comment: { body: "test-value" } });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/tasks/{task}/comments returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.comments.create("test-value", { comment: { body: "test-value" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/tasks/{task}/comments returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.comments.create("test-value", { comment: { body: "test-value" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/tasks/{task}/comments returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.comments.create("test-value", { comment: { body: "test-value" } });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/tasks/{task}/comments/{comment} returns successfully", async () => {
      const result = await client.v1.tasks.comments.delete("test-value", "test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/tasks/{task}/comments/{comment} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.comments.delete("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/tasks/{task}/comments/{comment} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.tasks.comments.delete("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/tasks/{task}/comments/{comment} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.comments.delete("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/tasks/{task}/comments/{comment} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.comments.delete("test-value", "test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("PUT /api/v1/tasks/{task}/comments/{comment} returns successfully", async () => {
      const result = await client.v1.tasks.comments.replace("test-value", "test-value", { body: "test-value" });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/tasks/{task}/comments/{comment} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.comments.replace("test-value", "test-value", { body: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/tasks/{task}/comments/{comment} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.tasks.comments.replace("test-value", "test-value", { body: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PUT /api/v1/tasks/{task}/comments/{comment} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.comments.replace("test-value", "test-value", { body: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/tasks/{task}/comments/{comment} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.comments.replace("test-value", "test-value", { body: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

  describe("tasks > lease", () => {
    it("DELETE /api/v1/tasks/{task}/lease returns successfully", async () => {
      const result = await client.v1.tasks.lease.remove("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/tasks/{task}/lease returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.lease.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/tasks/{task}/lease returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.tasks.lease.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/tasks/{task}/lease returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.lease.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/tasks/{task}/lease returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.tasks.lease.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("DELETE /api/v1/tasks/{task}/lease returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.lease.remove("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/tasks/{task}/lease returns successfully", async () => {
      const result = await client.v1.tasks.lease.list("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/tasks/{task}/lease returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.lease.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/tasks/{task}/lease returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.tasks.lease.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/tasks/{task}/lease returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.lease.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/tasks/{task}/lease returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.lease.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/tasks/{task}/lease returns successfully", async () => {
      const result = await client.v1.tasks.lease.create("test-value", { harness: "test-value", lease_id: "test-id", session_id: "test-id", session_name: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/tasks/{task}/lease returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.lease.create("test-value", { harness: "test-value", lease_id: "test-id", session_id: "test-id", session_name: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/tasks/{task}/lease returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.tasks.lease.create("test-value", { harness: "test-value", lease_id: "test-id", session_id: "test-id", session_name: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/tasks/{task}/lease returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.lease.create("test-value", { harness: "test-value", lease_id: "test-id", session_id: "test-id", session_name: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/tasks/{task}/lease returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.tasks.lease.create("test-value", { harness: "test-value", lease_id: "test-id", session_id: "test-id", session_name: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/tasks/{task}/lease returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.lease.create("test-value", { harness: "test-value", lease_id: "test-id", session_id: "test-id", session_name: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/tasks/{task}/lease/renew returns successfully", async () => {
      const result = await client.v1.tasks.lease.renew("test-value", { lease_id: "test-id", session_id: "test-id" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/tasks/{task}/lease/renew returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.tasks.lease.renew("test-value", { lease_id: "test-id", session_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/tasks/{task}/lease/renew returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.tasks.lease.renew("test-value", { lease_id: "test-id", session_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/tasks/{task}/lease/renew returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.tasks.lease.renew("test-value", { lease_id: "test-id", session_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/tasks/{task}/lease/renew returns 409", async () => {
      const ec = errorClient(409);
      try {
        await ec.v1.tasks.lease.renew("test-value", { lease_id: "test-id", session_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(409);
      }
    });

    it("POST /api/v1/tasks/{task}/lease/renew returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.tasks.lease.renew("test-value", { lease_id: "test-id", session_id: "test-id" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
