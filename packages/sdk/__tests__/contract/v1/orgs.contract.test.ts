// Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 19ebcf0ff5db


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: orgs", () => {
  describe("orgs", () => {
    it("GET /api/v1/orgs returns successfully", async () => {
      const result = await client.v1.orgs.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/orgs returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.orgs.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/orgs returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.orgs.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/orgs/{org}/artifacts returns successfully", async () => {
      const result = await client.v1.orgs.artifacts("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/orgs/{org}/artifacts returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.orgs.artifacts("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/orgs/{org}/artifacts returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.orgs.artifacts("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/orgs/{org}/artifacts returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.orgs.artifacts("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/orgs/{org}/artifacts returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.orgs.artifacts("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/orgs/{org}/linked_accounts returns successfully", async () => {
      const result = await client.v1.orgs.linkedAccounts("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/orgs/{org}/linked_accounts returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.orgs.linkedAccounts("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/orgs/{org}/linked_accounts returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.orgs.linkedAccounts("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/orgs/{org}/linked_accounts returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.orgs.linkedAccounts("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

  });

  describe("orgs > threads", () => {
    it("GET /api/v1/orgs/{org}/threads returns successfully", async () => {
      const result = await client.v1.orgs.threads.list("test-value");
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/orgs/{org}/threads returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.orgs.threads.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/orgs/{org}/threads returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.orgs.threads.list("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/orgs/{org}/threads returns successfully", async () => {
      const result = await client.v1.orgs.threads.create("test-value", { thread: {} });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/orgs/{org}/threads returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.orgs.threads.create("test-value", { thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/orgs/{org}/threads returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.orgs.threads.create("test-value", { thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/orgs/{org}/threads returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.orgs.threads.create("test-value", { thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/orgs/{org}/threads returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.orgs.threads.create("test-value", { thread: {} });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
