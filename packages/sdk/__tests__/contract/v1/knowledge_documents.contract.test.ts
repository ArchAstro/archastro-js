// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: df0a57f87c77


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: knowledge_documents", () => {
  describe("knowledge_documents", () => {
    it("GET /api/v1/knowledge_documents returns successfully", async () => {
      const result = await client.v1.knowledge_documents.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/knowledge_documents returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_documents.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/knowledge_documents returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_documents.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/knowledge_documents/{document} returns successfully", async () => {
      const result = await client.v1.knowledge_documents.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/knowledge_documents/{document} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_documents.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/knowledge_documents/{document} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_documents.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/knowledge_documents/{document} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.knowledge_documents.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/knowledge_documents/{document} returns successfully", async () => {
      const result = await client.v1.knowledge_documents.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/knowledge_documents/{document} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_documents.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/knowledge_documents/{document} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_documents.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/knowledge_documents/{document} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.knowledge_documents.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/knowledge_documents/{document}/content returns successfully", async () => {
      const result = await client.v1.knowledge_documents.content("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/knowledge_documents/{document}/content returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_documents.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/knowledge_documents/{document}/content returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_documents.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/knowledge_documents/{document}/content returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.knowledge_documents.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/knowledge_documents/{document}/content returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.knowledge_documents.content("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
