// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 9e4e02894115


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: knowledge_sources", () => {
  describe("knowledge_sources", () => {
    it("GET /api/v1/knowledge_sources returns successfully", async () => {
      const result = await client.v1.knowledge_sources.list();
      expect(result).toBeDefined();
      expect(result).toHaveProperty("data");
      expect(Array.isArray((result as any).data)).toBe(true);
    });

    it("GET /api/v1/knowledge_sources returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_sources.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/knowledge_sources returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_sources.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/knowledge_sources returns successfully", async () => {
      const result = await client.v1.knowledge_sources.create({ type: "test" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/knowledge_sources returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_sources.create({ type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/knowledge_sources returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_sources.create({ type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/knowledge_sources returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.knowledge_sources.create({ type: "test" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/knowledge_sources/{source} returns successfully", async () => {
      const result = await client.v1.knowledge_sources.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/knowledge_sources/{source} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_sources.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/knowledge_sources/{source} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_sources.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/knowledge_sources/{source} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.knowledge_sources.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/knowledge_sources/{source} returns successfully", async () => {
      const result = await client.v1.knowledge_sources.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/knowledge_sources/{source} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_sources.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/knowledge_sources/{source} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_sources.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/knowledge_sources/{source} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.knowledge_sources.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/knowledge_sources/{source} returns successfully", async () => {
      const result = await client.v1.knowledge_sources.update("test-value", {});
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/knowledge_sources/{source} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_sources.update("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/knowledge_sources/{source} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_sources.update("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PATCH /api/v1/knowledge_sources/{source} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.knowledge_sources.update("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/knowledge_sources/{source} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.knowledge_sources.update("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/knowledge_sources/{source}/ingest returns successfully", async () => {
      const result = await client.v1.knowledge_sources.ingest("test-value", {});
      expect(result).toBeDefined();
    });

    it("POST /api/v1/knowledge_sources/{source}/ingest returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_sources.ingest("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/knowledge_sources/{source}/ingest returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_sources.ingest("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/knowledge_sources/{source}/ingest returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.knowledge_sources.ingest("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/knowledge_sources/{source}/ingest returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.knowledge_sources.ingest("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/knowledge_sources/{source}/ingest returns 429", async () => {
      const ec = errorClient(429);
      try {
        await ec.v1.knowledge_sources.ingest("test-value", {});
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(429);
      }
    });

  });

  describe("knowledge_sources > kinds", () => {
    it("GET /api/v1/knowledge_sources/kinds returns successfully", async () => {
      const result = await client.v1.knowledge_sources.kinds.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/knowledge_sources/kinds returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.knowledge_sources.kinds.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/knowledge_sources/kinds returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.knowledge_sources.kinds.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

  });

});
