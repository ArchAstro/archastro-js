// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: 06cf7043fb87


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: kv", () => {
  describe("kv", () => {
    it("DELETE /api/v1/kv returns successfully", async () => {
      const result = await client.v1.kv.remove();
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/kv returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.kv.remove();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("DELETE /api/v1/kv returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.kv.remove();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/kv returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.kv.remove();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/kv returns successfully", async () => {
      const result = await client.v1.kv.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/kv returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.kv.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/kv returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.kv.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/kv returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.kv.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/kv returns successfully", async () => {
      const result = await client.v1.kv.create({ value: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/kv returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.kv.create({ value: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("POST /api/v1/kv returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.kv.create({ value: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/kv returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.kv.create({ value: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("DELETE /api/v1/kv/{key} returns successfully", async () => {
      const result = await client.v1.kv.delete("test-key");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/kv/{key} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.kv.delete("test-key");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("DELETE /api/v1/kv/{key} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.kv.delete("test-key");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/kv/{key} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.kv.delete("test-key");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/kv/{key} returns successfully", async () => {
      const result = await client.v1.kv.get("test-key");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/kv/{key} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.kv.get("test-key");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("GET /api/v1/kv/{key} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.kv.get("test-key");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/kv/{key} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.kv.get("test-key");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PUT /api/v1/kv/{key} returns successfully", async () => {
      const result = await client.v1.kv.upsert("test-key", { value: "test-value" });
      expect(result).toBeDefined();
    });

    it("PUT /api/v1/kv/{key} returns 400", async () => {
      const ec = errorClient(400);
      try {
        await ec.v1.kv.upsert("test-key", { value: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(400);
      }
    });

    it("PUT /api/v1/kv/{key} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.kv.upsert("test-key", { value: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PUT /api/v1/kv/{key} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.kv.upsert("test-key", { value: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
