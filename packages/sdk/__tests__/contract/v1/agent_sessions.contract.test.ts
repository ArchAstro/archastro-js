// Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
// This file is auto-generated by @archastro/sdk-generator. Do not edit.
// Content hash: b33f474b3642


import { describe, it, expect } from "vitest";
import { PlatformClient } from "../../../src/index.js";
import { ApiError } from "../../../src/runtime/http-client.js";

const PRISM_URL = process.env.PRISM_URL ?? "http://127.0.0.1:4040";

const client = new PlatformClient({
  baseUrl: PRISM_URL,
  defaultHeaders: { "x-archastro-api-key": "pk_test-key" },
  accessToken: "test-token",
});

function errorClient(code: number): PlatformClient {
  return new PlatformClient({
    baseUrl: PRISM_URL,
    defaultHeaders: { "x-archastro-api-key": "pk_test-key", "Prefer": `code=${code}` },
    accessToken: "test-token",
  });
}

describe("contract: agent_sessions", () => {
  describe("agent_sessions", () => {
    it("POST /api/v1/agent_sessions/{agent_session}/cancel returns successfully", async () => {
      const result = await client.v1.agent_sessions.cancel("test-value", { agent_session: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_sessions/{agent_session}/cancel returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_sessions.cancel("test-value", { agent_session: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_sessions/{agent_session}/cancel returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_sessions.cancel("test-value", { agent_session: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_sessions/{agent_session}/cancel returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_sessions.cancel("test-value", { agent_session: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("DELETE /api/v1/agent_sessions/{agent_session} returns successfully", async () => {
      const result = await client.v1.agent_sessions.delete("test-value");
      expect(result).toBeUndefined();
    });

    it("DELETE /api/v1/agent_sessions/{agent_session} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_sessions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("DELETE /api/v1/agent_sessions/{agent_session} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_sessions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("DELETE /api/v1/agent_sessions/{agent_session} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_sessions.delete("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("GET /api/v1/agent_sessions/{agent_session} returns successfully", async () => {
      const result = await client.v1.agent_sessions.get("test-value");
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_sessions/{agent_session} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_sessions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_sessions/{agent_session} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_sessions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_sessions/{agent_session} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_sessions.get("test-value");
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agent_sessions/{agent_session} returns successfully", async () => {
      const result = await client.v1.agent_sessions.update("test-value", { agent_session: "test-value" });
      expect(result).toBeDefined();
    });

    it("PATCH /api/v1/agent_sessions/{agent_session} returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_sessions.update("test-value", { agent_session: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("PATCH /api/v1/agent_sessions/{agent_session} returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_sessions.update("test-value", { agent_session: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("PATCH /api/v1/agent_sessions/{agent_session} returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_sessions.update("test-value", { agent_session: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("PATCH /api/v1/agent_sessions/{agent_session} returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_sessions.update("test-value", { agent_session: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("GET /api/v1/agent_sessions returns successfully", async () => {
      const result = await client.v1.agent_sessions.list();
      expect(result).toBeDefined();
    });

    it("GET /api/v1/agent_sessions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_sessions.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("GET /api/v1/agent_sessions returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_sessions.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("GET /api/v1/agent_sessions returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_sessions.list();
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_sessions returns successfully", async () => {
      const result = await client.v1.agent_sessions.create({ agent: "test-value", instructions: "test-value" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_sessions returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_sessions.create({ agent: "test-value", instructions: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_sessions returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_sessions.create({ agent: "test-value", instructions: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_sessions returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_sessions.create({ agent: "test-value", instructions: "test-value" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

    it("POST /api/v1/agent_sessions/{agent_session}/message returns successfully", async () => {
      const result = await client.v1.agent_sessions.message("test-value", { agent_session: "test-value", content: "test content" });
      expect(result).toBeDefined();
    });

    it("POST /api/v1/agent_sessions/{agent_session}/message returns 401", async () => {
      const ec = errorClient(401);
      try {
        await ec.v1.agent_sessions.message("test-value", { agent_session: "test-value", content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(401);
      }
    });

    it("POST /api/v1/agent_sessions/{agent_session}/message returns 403", async () => {
      const ec = errorClient(403);
      try {
        await ec.v1.agent_sessions.message("test-value", { agent_session: "test-value", content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(403);
      }
    });

    it("POST /api/v1/agent_sessions/{agent_session}/message returns 404", async () => {
      const ec = errorClient(404);
      try {
        await ec.v1.agent_sessions.message("test-value", { agent_session: "test-value", content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(404);
      }
    });

    it("POST /api/v1/agent_sessions/{agent_session}/message returns 422", async () => {
      const ec = errorClient(422);
      try {
        await ec.v1.agent_sessions.message("test-value", { agent_session: "test-value", content: "test content" });
        expect.fail("Expected ApiError to be thrown");
      } catch (e) {
        expect(e).toBeInstanceOf(ApiError);
        expect((e as ApiError).status).toBe(422);
      }
    });

  });

});
